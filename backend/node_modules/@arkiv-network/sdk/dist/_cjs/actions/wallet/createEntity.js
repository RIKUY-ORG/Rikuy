"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createEntity = createEntity;
const arkivTransactions_1 = require("../../utils/arkivTransactions");
async function createEntity(client, data, txParams) {
    console.debug("createEntity", data);
    const txData = (0, arkivTransactions_1.opsToTxData)({ creates: [data] });
    const receipt = await (0, arkivTransactions_1.sendArkivTransaction)(client, txData, txParams);
    console.debug("Receipt from createEntity", receipt);
    return {
        txHash: receipt.transactionHash,
        entityKey: receipt.logs[0].topics[1],
    };
}
//# sourceMappingURL=createEntity.js.map
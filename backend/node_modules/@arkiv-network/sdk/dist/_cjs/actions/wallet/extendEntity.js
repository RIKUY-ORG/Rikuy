"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.extendEntity = extendEntity;
const arkivTransactions_1 = require("../../utils/arkivTransactions");
async function extendEntity(client, data, txParams) {
    console.debug("extendEntity", data);
    const txData = (0, arkivTransactions_1.opsToTxData)({ extensions: [data] });
    const receipt = await (0, arkivTransactions_1.sendArkivTransaction)(client, txData, txParams);
    console.debug("Receipt from extendEntity", receipt);
    return {
        txHash: receipt.transactionHash,
        entityKey: receipt.logs[0].topics[1],
    };
}
//# sourceMappingURL=extendEntity.js.map
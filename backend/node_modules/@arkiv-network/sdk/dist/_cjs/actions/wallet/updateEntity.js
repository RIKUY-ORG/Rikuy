"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateEntity = updateEntity;
const arkivTransactions_1 = require("../../utils/arkivTransactions");
async function updateEntity(client, data, txParams) {
    console.debug("updateEntity", data);
    const txData = (0, arkivTransactions_1.opsToTxData)({ updates: [data] });
    const receipt = await (0, arkivTransactions_1.sendArkivTransaction)(client, txData, txParams);
    console.debug("Receipt from updateEntity", receipt);
    return {
        txHash: receipt.transactionHash,
        entityKey: receipt.logs[0].topics[1],
    };
}
//# sourceMappingURL=updateEntity.js.map
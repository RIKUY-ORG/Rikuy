import { changeOwnership } from "../../actions/wallet/changeOwnership";
import { createEntity, } from "../../actions/wallet/createEntity";
import { deleteEntity } from "../../actions/wallet/deleteEntity";
import { extendEntity } from "../../actions/wallet/extendEntity";
import { mutateEntities } from "../../actions/wallet/mutateEntities";
import { updateEntity } from "../../actions/wallet/updateEntity";
export function walletArkivActions(client) {
    return {
        createEntity: (data, txParams) => createEntity(client, data, txParams),
        updateEntity: (data, txParams) => updateEntity(client, data, txParams),
        deleteEntity: (data, txParams) => deleteEntity(client, data, txParams),
        extendEntity: (data, txParams) => extendEntity(client, data, txParams),
        changeOwnership: (data, txParams) => changeOwnership(client, data, txParams),
        mutateEntities: (data, txParams) => mutateEntities(client, data, txParams),
    };
}
//# sourceMappingURL=arkivWallet.js.map
import { toBytes } from "viem";
import { Entity } from "../types/entity";
export async function entityFromRpcResult(rpcEntity) {
    return new Entity(rpcEntity.key, rpcEntity.contentType, rpcEntity.owner, rpcEntity.expiresAt, toBytes(rpcEntity.value), [
        ...(rpcEntity.stringAttributes ?? []).map(({ key, value }) => ({
            key,
            value,
        })),
        ...(rpcEntity.numericAttributes ?? []).map(({ key, value }) => ({
            key,
            value: Number(value),
        })),
    ]);
}
//# sourceMappingURL=entities.js.map
import { toBytes } from "viem";
export function jsonToPayload(json) {
    return toBytes(JSON.stringify(json));
}
export function stringToPayload(data) {
    return toBytes(data);
}
//# sourceMappingURL=payload.js.map
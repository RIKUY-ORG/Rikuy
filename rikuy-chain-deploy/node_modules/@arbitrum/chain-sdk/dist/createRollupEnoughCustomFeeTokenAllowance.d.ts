import { Address, PublicClient, Transport, Chain } from 'viem';
import { Prettify } from './types/utils';
import { RollupCreatorSupportedVersion } from './types/createRollupTypes';
export type CreateRollupEnoughCustomFeeTokenAllowanceParams<TChain extends Chain | undefined> = Prettify<{
    nativeToken: Address;
    maxFeePerGasForRetryables?: bigint;
    account: Address;
    publicClient: PublicClient<Transport, TChain>;
    rollupCreatorAddressOverride?: Address;
    rollupCreatorVersion?: RollupCreatorSupportedVersion;
}>;
export declare function createRollupEnoughCustomFeeTokenAllowance<TChain extends Chain | undefined>({ nativeToken, maxFeePerGasForRetryables, account, publicClient, rollupCreatorAddressOverride, rollupCreatorVersion, }: CreateRollupEnoughCustomFeeTokenAllowanceParams<TChain>): Promise<boolean>;

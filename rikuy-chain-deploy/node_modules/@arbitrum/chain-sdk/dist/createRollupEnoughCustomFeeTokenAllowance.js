"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRollupEnoughCustomFeeTokenAllowance = void 0;
const erc20_1 = require("./utils/erc20");
const getRollupCreatorAddress_1 = require("./utils/getRollupCreatorAddress");
const createRollupGetRetryablesFees_1 = require("./createRollupGetRetryablesFees");
const decimals_1 = require("./utils/decimals");
async function createRollupEnoughCustomFeeTokenAllowance({ nativeToken, maxFeePerGasForRetryables, account, publicClient, rollupCreatorAddressOverride, rollupCreatorVersion = 'v3.1', }) {
    const allowance = await (0, erc20_1.fetchAllowance)({
        address: nativeToken,
        owner: account,
        spender: rollupCreatorAddressOverride ?? (0, getRollupCreatorAddress_1.getRollupCreatorAddress)(publicClient, rollupCreatorVersion),
        publicClient,
    });
    const fees = await (0, createRollupGetRetryablesFees_1.createRollupGetRetryablesFeesWithDefaults)(publicClient, {
        account,
        nativeToken,
        maxFeePerGasForRetryables,
    }, rollupCreatorVersion);
    const decimals = await (0, erc20_1.fetchDecimals)({
        address: nativeToken,
        publicClient,
    });
    return allowance >= (0, decimals_1.scaleFrom18DecimalsToNativeTokenDecimals)({ amount: fees, decimals });
}
exports.createRollupEnoughCustomFeeTokenAllowance = createRollupEnoughCustomFeeTokenAllowance;

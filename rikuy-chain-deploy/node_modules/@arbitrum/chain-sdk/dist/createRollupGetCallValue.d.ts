import { Chain, PublicClient, Transport, Address } from 'viem';
import { RollupCreatorSupportedVersion } from './types/createRollupTypes';
export declare function createRollupGetCallValue<TChain extends Chain | undefined>(publicClient: PublicClient<Transport, TChain>, params: {
    account: Address;
    nativeToken: Address;
    deployFactoriesToL2: boolean;
}, rollupCreatorVersion?: RollupCreatorSupportedVersion): Promise<bigint>;

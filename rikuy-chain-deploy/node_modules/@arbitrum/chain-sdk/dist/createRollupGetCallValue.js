"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRollupGetCallValue = void 0;
const createRollupGetRetryablesFees_1 = require("./createRollupGetRetryablesFees");
const isNonZeroAddress_1 = require("./utils/isNonZeroAddress");
async function createRollupGetCallValue(publicClient, params, rollupCreatorVersion = 'v3.1') {
    // when not deploying deterministic factories to L2, no callvalue is necessary, as no retryable tickets will be created
    if (!params.deployFactoriesToL2) {
        return BigInt(0);
    }
    // when using a custom fee token, the retryable tickets will be paid for in the custom fee token, so no callvalue is necessary
    if ((0, isNonZeroAddress_1.isNonZeroAddress)(params.nativeToken)) {
        return BigInt(0);
    }
    return (0, createRollupGetRetryablesFees_1.createRollupGetRetryablesFeesWithDefaults)(publicClient, params, rollupCreatorVersion);
}
exports.createRollupGetCallValue = createRollupGetCallValue;

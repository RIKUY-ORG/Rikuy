"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const viem_1 = require("viem");
const chains_1 = require("viem/chains");
const utils_1 = require("./utils");
const prepareChainConfig_1 = require("./prepareChainConfig");
const constants_1 = require("./constants");
const createRollupPrepareDeploymentParamsConfig_1 = require("./createRollupPrepareDeploymentParamsConfig");
const createRollupPrepareTransactionRequest_1 = require("./createRollupPrepareTransactionRequest");
const RollupCreator_1 = require("./contracts/RollupCreator");
const testHelpers_1 = require("./testHelpers");
const chains_2 = require("./chains");
const wasmModuleRoot_1 = require("./wasmModuleRoot");
const testnodeAccounts = (0, testHelpers_1.getNitroTestnodePrivateKeyAccounts)();
const deployer = testnodeAccounts.deployer;
const publicClient = (0, viem_1.createPublicClient)({
    chain: chains_1.arbitrumSepolia,
    transport: (0, viem_1.http)(),
});
(0, vitest_1.it)(`fails to prepare transaction request if "params.batchPosters" is set to an empty array`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address },
    });
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [],
            validators: [deployer.address],
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`"params.batchPosters" can't be empty or contain the zero address.`);
});
(0, vitest_1.it)(`fails to prepare transaction request if "params.batchPosters" includes the zero address`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address },
    });
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            // set batch posters array to include zero address
            batchPosters: [viem_1.zeroAddress, deployer.address],
            validators: [deployer.address],
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`"params.batchPosters" can't be empty or contain the zero address.`);
});
(0, vitest_1.it)(`fails to prepare transaction request if "params.validators" is set to an empty array`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address },
    });
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            // set validators to an empty array
            validators: [],
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`"params.validators" can't be empty or contain the zero address.`);
});
(0, vitest_1.it)(`fails to prepare transaction request if "params.validators" includes the zero address`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address },
    });
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            // set validators to zero address
            validators: [viem_1.zeroAddress],
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`"params.validators" can't be empty or contain the zero address.`);
});
(0, vitest_1.it)(`fails to prepare transaction request if "params.nativeToken" is custom, is Rollup chain and "params.feeTokenPricer" is not provided`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address },
    });
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
            // set native token to anything custom
            nativeToken: deployer.address,
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`"params.feeTokenPricer" must be provided for a custom gas token rollup chain.`);
});
(0, vitest_1.it)(`fails to prepare transaction request if ArbOS version 30 is selected`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, InitialArbOSVersion: 30 },
    });
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`ArbOS 30 is not supported. Please set the ArbOS version to 32 or later by updating "arbitrum.InitialArbOSVersion" in your chain config.`);
});
(0, vitest_1.it)(`fails to prepare transaction request if ArbOS version 31 is selected`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, InitialArbOSVersion: 31 },
    });
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`ArbOS 31 is not supported. Please set the ArbOS version to 32 or later by updating "arbitrum.InitialArbOSVersion" in your chain config.`);
});
(0, vitest_1.it)(`fails to prepare transaction request if ArbOS version is incompatible with Consensus version`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, InitialArbOSVersion: 32 },
    });
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
                wasmModuleRoot: (0, wasmModuleRoot_1.getConsensusReleaseByVersion)(20).wasmModuleRoot,
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`Consensus v20 does not support ArbOS 32. Please update your "wasmModuleRoot" to that of a Consensus version compatible with ArbOS 32.`);
});
(0, vitest_1.it)(`fails to prepare transaction request if "params.maxDataSize" is not provided for a custom parent chain`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, DataAvailabilityCommittee: true },
    });
    const chain = (0, testHelpers_1.testHelper_createCustomParentChain)({
        id: chainId,
    });
    const publicClient = (0, viem_1.createPublicClient)({
        chain,
        transport: (0, viem_1.http)(),
    });
    (0, chains_2.registerCustomParentChain)(chain);
    // prepare the transaction for deploying the core contracts
    await (0, vitest_1.expect)((0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
                confirmPeriodBlocks: 1n,
                challengeGracePeriodBlocks: 2n,
                minimumAssertionPeriod: 3n,
                validatorAfkBlocks: 4n,
                sequencerInboxMaxTimeVariation: {
                    delayBlocks: 2n,
                    futureBlocks: 3n,
                    delaySeconds: 4n,
                    futureSeconds: 5n,
                },
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
        },
        account: deployer.address,
        publicClient,
    })).rejects.toThrowError(`"params.maxDataSize" must be provided when using a custom parent chain.`);
});
(0, vitest_1.it)(`successfully prepares a transaction request with the default rollup creator and a gas limit override`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, DataAvailabilityCommittee: true },
    });
    const txRequest = await (0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
        },
        value: constants_1.createRollupDefaultRetryablesFees,
        account: deployer.address,
        publicClient,
        gasOverrides: { gasLimit: { base: 1000n } },
    });
    (0, vitest_1.expect)(txRequest.account).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.from).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.to).toEqual(RollupCreator_1.rollupCreatorAddress[chains_1.arbitrumSepolia.id]);
    (0, vitest_1.expect)(txRequest.chainId).toEqual(chains_1.arbitrumSepolia.id);
    (0, vitest_1.expect)(txRequest.gas).toEqual(1000n);
});
(0, vitest_1.it)(`successfully prepares a transaction request with a custom rollup creator and a gas limit override`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, DataAvailabilityCommittee: true },
    });
    const txRequest = await (0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
        },
        account: deployer.address,
        value: constants_1.createRollupDefaultRetryablesFees,
        publicClient,
        gasOverrides: { gasLimit: { base: 1000n, percentIncrease: 20n } },
        rollupCreatorAddressOverride: '0x31421C442c422BD16aef6ae44D3b11F404eeaBd9',
    });
    (0, vitest_1.expect)(txRequest.account).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.from).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.to).toEqual('0x31421C442c422BD16aef6ae44D3b11F404eeaBd9');
    (0, vitest_1.expect)(txRequest.chainId).toEqual(chains_1.arbitrumSepolia.id);
    (0, vitest_1.expect)(txRequest.gas).toEqual(1200n);
});
(0, vitest_1.it)(`successfully prepares a transaction request with a custom parent chain`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, DataAvailabilityCommittee: true },
    });
    const chain = (0, testHelpers_1.testHelper_createCustomParentChain)({
        id: chainId,
    });
    const publicClient = (0, viem_1.createPublicClient)({
        chain,
        transport: (0, viem_1.http)(),
    });
    (0, chains_2.registerCustomParentChain)(chain);
    const txRequest = await (0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
                confirmPeriodBlocks: 1n,
                challengeGracePeriodBlocks: 2n,
                minimumAssertionPeriod: 3n,
                validatorAfkBlocks: 4n,
                sequencerInboxMaxTimeVariation: {
                    delayBlocks: 2n,
                    futureBlocks: 3n,
                    delaySeconds: 4n,
                    futureSeconds: 5n,
                },
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
            maxDataSize: 123456n,
        },
        account: deployer.address,
        value: constants_1.createRollupDefaultRetryablesFees,
        publicClient,
        gasOverrides: { gasLimit: { base: 1000n } },
    });
    (0, vitest_1.expect)(txRequest.account).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.from).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.to).toEqual(chain.contracts.rollupCreator.address);
    (0, vitest_1.expect)(txRequest.chainId).toEqual(chainId);
    (0, vitest_1.expect)(txRequest.gas).toEqual(1000n);
});
(0, vitest_1.it)(`successfully prepares a transaction request with a custom gas token AnyTrust chain`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, DataAvailabilityCommittee: true },
    });
    const txRequest = await (0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
            nativeToken: '0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d', // USDC
        },
        value: constants_1.createRollupDefaultRetryablesFees,
        account: deployer.address,
        publicClient,
        gasOverrides: { gasLimit: { base: 1000n } },
    });
    (0, vitest_1.expect)(txRequest.account).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.from).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.to).toEqual(RollupCreator_1.rollupCreatorAddress[chains_1.arbitrumSepolia.id]);
    (0, vitest_1.expect)(txRequest.chainId).toEqual(chains_1.arbitrumSepolia.id);
    (0, vitest_1.expect)(txRequest.gas).toEqual(1000n);
});
(0, vitest_1.it)(`successfully prepares a transaction request with a custom gas token Rollup chain`, async () => {
    // generate a random chain id
    const chainId = (0, utils_1.generateChainId)();
    // create the chain config
    const chainConfig = (0, prepareChainConfig_1.prepareChainConfig)({
        chainId,
        arbitrum: { InitialChainOwner: deployer.address, DataAvailabilityCommittee: false },
    });
    const txRequest = await (0, createRollupPrepareTransactionRequest_1.createRollupPrepareTransactionRequest)({
        params: {
            config: (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(publicClient, {
                chainId: BigInt(chainId),
                owner: deployer.address,
                chainConfig,
            }),
            batchPosters: [deployer.address],
            validators: [deployer.address],
            nativeToken: '0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d',
            feeTokenPricer: '0x31421C442c422BD16aef6ae44D3b11F404eeaBd9', // some address for the fee token pricer
        },
        value: constants_1.createRollupDefaultRetryablesFees,
        account: deployer.address,
        publicClient,
        gasOverrides: { gasLimit: { base: 1000n } },
    });
    (0, vitest_1.expect)(txRequest.account).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.from).toEqual(deployer.address);
    (0, vitest_1.expect)(txRequest.to).toEqual(RollupCreator_1.rollupCreatorAddress[chains_1.arbitrumSepolia.id]);
    (0, vitest_1.expect)(txRequest.chainId).toEqual(chains_1.arbitrumSepolia.id);
    (0, vitest_1.expect)(txRequest.gas).toEqual(1000n);
});

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRollupPrepareTransactionRequest = void 0;
const viem_1 = require("viem");
const createRollupDefaults_1 = require("./createRollupDefaults");
const createRollupGetCallValue_1 = require("./createRollupGetCallValue");
const createRollupGetMaxDataSize_1 = require("./createRollupGetMaxDataSize");
const createRollupEncodeFunctionData_1 = require("./createRollupEncodeFunctionData");
const ParentChain_1 = require("./types/ParentChain");
const isNonZeroAddress_1 = require("./utils/isNonZeroAddress");
const getRollupCreatorAddress_1 = require("./utils/getRollupCreatorAddress");
const erc20_1 = require("./utils/erc20");
const gasOverrides_1 = require("./utils/gasOverrides");
const wasmModuleRoot_1 = require("./wasmModuleRoot");
async function createRollupPrepareTransactionRequest({ params, account, value, publicClient, gasOverrides, rollupCreatorAddressOverride, rollupCreatorVersion = 'v3.1', }) {
    const { chainId: parentChainId, isCustom: parentChainIsCustom } = (0, ParentChain_1.validateParentChain)(publicClient);
    if (params.batchPosters.length === 0 || params.batchPosters.includes(viem_1.zeroAddress)) {
        throw new Error(`"params.batchPosters" can't be empty or contain the zero address.`);
    }
    if (params.validators.length === 0 || params.validators.includes(viem_1.zeroAddress)) {
        throw new Error(`"params.validators" can't be empty or contain the zero address.`);
    }
    const chainConfig = JSON.parse(params.config.chainConfig);
    if ((0, isNonZeroAddress_1.isNonZeroAddress)(params.nativeToken)) {
        const isRollupChain = chainConfig.arbitrum.DataAvailabilityCommittee === false;
        switch (rollupCreatorVersion) {
            case 'v2.1': {
                if (isRollupChain) {
                    throw new Error(`"params.nativeToken" can only be used on AnyTrust chains. Set "arbitrum.DataAvailabilityCommittee" to "true" in the chain config.`);
                }
                break;
            }
            case 'v3.1': {
                const isFeeTokenPricerMissing = !(0, isNonZeroAddress_1.isNonZeroAddress)(params.feeTokenPricer);
                // fee token pricer is mandatory for custom gas token rollup chains
                if (isRollupChain && isFeeTokenPricerMissing) {
                    throw new Error(`"params.feeTokenPricer" must be provided for a custom gas token rollup chain.`);
                }
            }
        }
        if ((await (0, erc20_1.fetchDecimals)({ address: params.nativeToken, publicClient })) > 36) {
            throw new Error(`"params.nativeToken" can only be configured with a token that uses 36 decimals or less.`);
        }
    }
    let maxDataSize;
    if (parentChainIsCustom) {
        if (typeof params.maxDataSize === 'undefined') {
            throw new Error(`"params.maxDataSize" must be provided when using a custom parent chain.`);
        }
        maxDataSize = params.maxDataSize;
    }
    else {
        maxDataSize = params.maxDataSize ?? (0, createRollupGetMaxDataSize_1.createRollupGetMaxDataSize)(parentChainId);
    }
    const arbOSVersion = chainConfig.arbitrum.InitialArbOSVersion;
    const wasmModuleRoot = params.config.wasmModuleRoot;
    if (arbOSVersion === 30 || arbOSVersion === 31) {
        throw new Error(`ArbOS ${arbOSVersion} is not supported. Please set the ArbOS version to 32 or later by updating "arbitrum.InitialArbOSVersion" in your chain config.`);
    }
    if ((0, wasmModuleRoot_1.isKnownWasmModuleRoot)(wasmModuleRoot)) {
        const consensusRelease = (0, wasmModuleRoot_1.getConsensusReleaseByWasmModuleRoot)(wasmModuleRoot);
        if (arbOSVersion > consensusRelease.maxArbOSVersion) {
            throw new Error(`Consensus v${consensusRelease.version} does not support ArbOS ${arbOSVersion}. Please update your "wasmModuleRoot" to that of a Consensus version compatible with ArbOS ${arbOSVersion}.`);
        }
    }
    const paramsWithDefaults = { ...createRollupDefaults_1.defaults, ...params, maxDataSize };
    const createRollupGetCallValueParams = { ...paramsWithDefaults, account };
    // @ts-ignore (todo: fix viem type issue)
    const request = await publicClient.prepareTransactionRequest({
        chain: publicClient.chain,
        to: rollupCreatorAddressOverride ?? (0, getRollupCreatorAddress_1.getRollupCreatorAddress)(publicClient, rollupCreatorVersion),
        data: (0, createRollupEncodeFunctionData_1.createRollupEncodeFunctionData)([paramsWithDefaults], rollupCreatorVersion),
        value: value ??
            (await (0, createRollupGetCallValue_1.createRollupGetCallValue)(publicClient, createRollupGetCallValueParams, rollupCreatorVersion)),
        account,
        // if the base gas limit override was provided, hardcode gas to 0 to skip estimation
        // we'll set the actual value in the code below
        gas: typeof gasOverrides?.gasLimit?.base !== 'undefined' ? 0n : undefined,
    });
    // potential gas overrides (gas limit)
    if (gasOverrides && gasOverrides.gasLimit) {
        request.gas = (0, gasOverrides_1.applyPercentIncrease)({
            // the ! is here because we should let it error in case we don't have the estimated gas
            base: gasOverrides.gasLimit.base ?? request.gas,
            percentIncrease: gasOverrides.gasLimit.percentIncrease,
        });
    }
    return { ...request, chainId: parentChainId };
}
exports.createRollupPrepareTransactionRequest = createRollupPrepareTransactionRequest;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createSafePrepareTransactionReceipt = void 0;
const viem_1 = require("viem");
const createSafePrepareTransactionRequest_1 = require("./createSafePrepareTransactionRequest");
function findProxyCreationEventLog(txReceipt) {
    const abiItem = (0, viem_1.getAbiItem)({ abi: createSafePrepareTransactionRequest_1.SafeProxyFactoryAbi, name: 'ProxyCreation' });
    const eventSelector = (0, viem_1.getEventSelector)(abiItem);
    const log = txReceipt.logs.find((log) => log.topics[0] === eventSelector);
    if (typeof log === 'undefined') {
        throw new Error(`No "ProxyCreation" logs found in logs for transaction: ${txReceipt.transactionHash}`);
    }
    return log;
}
function decodeProxyCreationEventLog(log) {
    const decodedEventLog = (0, viem_1.decodeEventLog)({ ...log, abi: createSafePrepareTransactionRequest_1.SafeProxyFactoryAbi });
    if (decodedEventLog.eventName !== 'ProxyCreation') {
        throw new Error(`Expected "ProxyCreation" event but found: ${decodedEventLog.eventName}`);
    }
    return decodedEventLog;
}
/**
 * Adds a getSafeContract() function to a regular {@link TransactionReceipt} for transactions that create a new Safe using the default SafeFactory
 */
function createSafePrepareTransactionReceipt(txReceipt) {
    return {
        ...txReceipt,
        getSafeContract: function () {
            const eventLog = findProxyCreationEventLog(txReceipt);
            const decodedEventLog = decodeProxyCreationEventLog(eventLog);
            const { proxy } = decodedEventLog.args;
            return proxy;
        },
    };
}
exports.createSafePrepareTransactionReceipt = createSafePrepareTransactionReceipt;

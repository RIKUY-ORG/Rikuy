"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getEstimateForSettingGateway = exports.createTokenBridgeGetInputs = void 0;
const ethers_1 = require("ethers");
const sdk_1 = require("@arbitrum/sdk");
const lib_1 = require("@arbitrum/sdk/dist/lib/utils/lib");
const RollupAdminLogic__factory_1 = require("@arbitrum/sdk/dist/lib/abi/factories/RollupAdminLogic__factory");
const L1AtomicTokenBridgeCreator_json_1 = __importDefault(require("@arbitrum/token-bridge-contracts/build/contracts/contracts/tokenbridge/ethereum/L1AtomicTokenBridgeCreator.sol/L1AtomicTokenBridgeCreator.json"));
const L2AtomicTokenBridgeFactory_json_1 = __importDefault(require("@arbitrum/token-bridge-contracts/build/contracts/contracts/tokenbridge/arbitrum/L2AtomicTokenBridgeFactory.sol/L2AtomicTokenBridgeFactory.json"));
const gasOverrides_1 = require("./utils/gasOverrides");
const registerNewNetwork_1 = require("./utils/registerNewNetwork");
const publicClientToProvider_1 = require("./ethers-compat/publicClientToProvider");
const NamedFactoryInstance = (contractJson) => {
    const factory = new ethers_1.ContractFactory(contractJson.abi, contractJson.bytecode);
    factory['contractName'] = contractJson.contractName;
    return factory;
};
// import from token-bridge-contracts directly to make sure the bytecode is the same
const L2AtomicTokenBridgeFactory__factory = NamedFactoryInstance(L2AtomicTokenBridgeFactory_json_1.default);
async function createTokenBridgeGetInputs(l1DeployerAddress, l1PublicClient, l2PublicClient, l1TokenBridgeCreatorAddress, rollupAddress, retryableGasOverrides) {
    const l1Provider = (0, publicClientToProvider_1.publicClientToProvider)(l1PublicClient);
    const l2Provider = (0, publicClientToProvider_1.publicClientToProvider)(l2PublicClient);
    await (0, registerNewNetwork_1.registerNewNetwork)(l1Provider, l2Provider, rollupAddress);
    //// run retryable estimate for deploying L2 factory
    const { maxSubmissionCost: maxSubmissionCostForFactoryEstimation, maxGas: maxGasForFactoryEstimation, } = await getEstimateForDeployingFactory(l1DeployerAddress, l1TokenBridgeCreatorAddress, l1Provider, l2Provider);
    const { maxSubmissionCost: maxSubmissionCostForContractsEstimation, maxGas: maxGasForContractsEstimation, } = await getEstimateForDeployingContracts(l1DeployerAddress, l1TokenBridgeCreatorAddress, l1Provider, l2Provider);
    //// apply gas overrides
    const maxSubmissionCostForFactory = retryableGasOverrides && retryableGasOverrides.maxSubmissionCostForFactory
        ? ethers_1.BigNumber.from((0, gasOverrides_1.applyPercentIncrease)({
            base: retryableGasOverrides.maxSubmissionCostForFactory.base ??
                BigInt(maxSubmissionCostForFactoryEstimation.toHexString()),
            percentIncrease: retryableGasOverrides.maxSubmissionCostForFactory.percentIncrease,
        }))
        : maxSubmissionCostForFactoryEstimation;
    const maxGasForFactory = retryableGasOverrides && retryableGasOverrides.maxGasForFactory
        ? ethers_1.BigNumber.from((0, gasOverrides_1.applyPercentIncrease)({
            base: retryableGasOverrides.maxGasForFactory.base ??
                BigInt(maxGasForFactoryEstimation.toHexString()),
            percentIncrease: retryableGasOverrides.maxGasForFactory.percentIncrease,
        }))
        : maxGasForFactoryEstimation;
    const maxSubmissionCostForContracts = retryableGasOverrides && retryableGasOverrides.maxSubmissionCostForContracts
        ? ethers_1.BigNumber.from((0, gasOverrides_1.applyPercentIncrease)({
            base: retryableGasOverrides.maxSubmissionCostForContracts.base ??
                BigInt(maxSubmissionCostForContractsEstimation.toHexString()),
            percentIncrease: retryableGasOverrides.maxSubmissionCostForContracts.percentIncrease,
        }))
        : maxSubmissionCostForContractsEstimation;
    const maxGasForContracts = retryableGasOverrides && retryableGasOverrides.maxGasForContracts
        ? ethers_1.BigNumber.from((0, gasOverrides_1.applyPercentIncrease)({
            base: retryableGasOverrides.maxGasForContracts.base ??
                BigInt(maxGasForContractsEstimation.toHexString()),
            percentIncrease: retryableGasOverrides.maxGasForContracts.percentIncrease,
        }))
        : maxGasForContractsEstimation;
    const maxGasPrice = retryableGasOverrides && retryableGasOverrides.maxGasPrice
        ? ethers_1.BigNumber.from(retryableGasOverrides.maxGasPrice)
        : await l2Provider.getGasPrice();
    let retryableFee = maxSubmissionCostForFactory
        .add(maxSubmissionCostForContracts)
        .add(maxGasForFactory.mul(maxGasPrice))
        .add(maxGasForContracts.mul(maxGasPrice));
    // get inbox from rollup contract
    const inbox = await RollupAdminLogic__factory_1.RollupAdminLogic__factory.connect(rollupAddress, l1Provider).inbox();
    return {
        inbox: inbox,
        maxGasForContracts: maxGasForContracts.toBigInt(),
        maxGasPrice: maxGasPrice.toBigInt(),
        retryableFee: retryableFee.toBigInt(),
    };
}
exports.createTokenBridgeGetInputs = createTokenBridgeGetInputs;
const getEstimateForDeployingFactory = async (l1DeployerAddress, l1TokenBridgeCreatorAddress, l1Provider, l2Provider) => {
    const L1AtomicTokenBridgeCreator__factory = new ethers_1.ethers.Contract(l1TokenBridgeCreatorAddress, L1AtomicTokenBridgeCreator_json_1.default.abi);
    const l1TokenBridgeCreator = L1AtomicTokenBridgeCreator__factory.connect(l1Provider);
    //// run retryable estimate for deploying L2 factory
    const l1ToL2MsgGasEstimate = new sdk_1.ParentToChildMessageGasEstimator(l2Provider);
    const { maxSubmissionCost } = await l1ToL2MsgGasEstimate.estimateAll({
        from: ethers_1.ethers.Wallet.createRandom().address,
        to: ethers_1.ethers.constants.AddressZero,
        l2CallValue: ethers_1.BigNumber.from(0),
        excessFeeRefundAddress: l1DeployerAddress,
        callValueRefundAddress: l1DeployerAddress,
        data: L2AtomicTokenBridgeFactory__factory.bytecode,
    }, await (0, lib_1.getBaseFee)(l1Provider), l1Provider);
    const maxGas = (await l1TokenBridgeCreator.gasLimitForL2FactoryDeployment());
    return {
        // there's already a 300% increase buffer in the SDK
        // https://github.com/OffchainLabs/arbitrum-sdk/blob/main/src/lib/message/ParentToChildMessageGasEstimator.ts#L27
        maxSubmissionCost,
        maxGas,
    };
};
async function getEstimateForDeployingContracts(l1DeployerAddress, l1TokenBridgeCreatorAddress, l1Provider, l2Provider) {
    const L1AtomicTokenBridgeCreator__factory = new ethers_1.ethers.Contract(l1TokenBridgeCreatorAddress, L1AtomicTokenBridgeCreator_json_1.default.abi);
    const l1TokenBridgeCreator = L1AtomicTokenBridgeCreator__factory.connect(l1Provider);
    const l2FactoryTemplate = L2AtomicTokenBridgeFactory__factory.attach(await l1TokenBridgeCreator.l2TokenBridgeFactoryTemplate()).connect(l1Provider);
    const l2Code = {
        router: await l1Provider.getCode(await l1TokenBridgeCreator.l2RouterTemplate()),
        standardGateway: await l1Provider.getCode(await l1TokenBridgeCreator.l2StandardGatewayTemplate()),
        customGateway: await l1Provider.getCode(await l1TokenBridgeCreator.l2CustomGatewayTemplate()),
        wethGateway: await l1Provider.getCode(await l1TokenBridgeCreator.l2WethGatewayTemplate()),
        aeWeth: await l1Provider.getCode(await l1TokenBridgeCreator.l2WethTemplate()),
        upgradeExecutor: await l1Provider.getCode((await l1TokenBridgeCreator.l1Templates()).upgradeExecutor),
        multicall: await l1Provider.getCode(await l1TokenBridgeCreator.l2MulticallTemplate()),
    };
    const l1ToL2MsgGasEstimate = new sdk_1.ParentToChildMessageGasEstimator(l2Provider);
    const calldata = l2FactoryTemplate.interface.encodeFunctionData('deployL2Contracts', [
        l2Code,
        ethers_1.ethers.Wallet.createRandom().address,
        ethers_1.ethers.Wallet.createRandom().address,
        ethers_1.ethers.Wallet.createRandom().address,
        ethers_1.ethers.Wallet.createRandom().address,
        ethers_1.ethers.Wallet.createRandom().address,
        ethers_1.ethers.Wallet.createRandom().address,
        ethers_1.ethers.Wallet.createRandom().address,
        ethers_1.ethers.Wallet.createRandom().address,
    ]);
    const maxSubmissionCost = await l1ToL2MsgGasEstimate.estimateSubmissionFee(l1Provider, await l1Provider.getGasPrice(), ethers_1.ethers.utils.hexDataLength(calldata));
    const maxGas = await l2FactoryTemplate.estimateGas.deployL2Contracts(l2Code, ethers_1.ethers.Wallet.createRandom().address, ethers_1.ethers.Wallet.createRandom().address, ethers_1.ethers.Wallet.createRandom().address, ethers_1.ethers.Wallet.createRandom().address, ethers_1.ethers.Wallet.createRandom().address, ethers_1.ethers.Wallet.createRandom().address, ethers_1.ethers.Wallet.createRandom().address, ethers_1.ethers.Wallet.createRandom().address);
    return {
        // there's already a 300% increase buffer in the SDK
        // https://github.com/OffchainLabs/arbitrum-sdk/blob/main/src/lib/message/ParentToChildMessageGasEstimator.ts#L27
        maxSubmissionCost,
        maxGas: maxGas.mul(2),
    };
}
async function getEstimateForSettingGateway(l1ChainOwnerAddress, l1UpgradeExecutorAddress, l1GatewayRouterAddress, setGatewaysCalldata, parentChainPublicClient, orbitChainPublicClient) {
    // ethers providers
    const parentChainProvider = (0, publicClientToProvider_1.publicClientToProvider)(parentChainPublicClient);
    const orbitChainProvider = (0, publicClientToProvider_1.publicClientToProvider)(orbitChainPublicClient);
    // run retryable estimate for setting a token gateway in the router
    const l1ToL2MsgGasEstimate = new sdk_1.ParentToChildMessageGasEstimator(orbitChainProvider);
    const setGatewaysGasParams = await l1ToL2MsgGasEstimate.estimateAll({
        from: l1UpgradeExecutorAddress,
        to: l1GatewayRouterAddress,
        l2CallValue: ethers_1.BigNumber.from(0),
        excessFeeRefundAddress: l1ChainOwnerAddress,
        callValueRefundAddress: l1ChainOwnerAddress,
        data: setGatewaysCalldata,
    }, await (0, lib_1.getBaseFee)(parentChainProvider), parentChainProvider);
    return {
        gasLimit: setGatewaysGasParams.gasLimit.toBigInt(),
        maxFeePerGas: setGatewaysGasParams.maxFeePerGas.toBigInt(),
        maxSubmissionCost: setGatewaysGasParams.maxSubmissionCost.toBigInt(),
        deposit: setGatewaysGasParams.deposit.toBigInt(),
    };
}
exports.getEstimateForSettingGateway = getEstimateForSettingGateway;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const viem_1 = require("viem");
const chains_1 = require("./chains");
const testHelpers_1 = require("./testHelpers");
const createTokenBridgePrepareTransactionRequest_1 = require("./createTokenBridgePrepareTransactionRequest");
const createTokenBridgePrepareTransactionReceipt_1 = require("./createTokenBridgePrepareTransactionReceipt");
const createTokenBridge_testHelpers_1 = require("./createTokenBridge-testHelpers");
const createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest_1 = require("./createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest");
const ERC20_1 = require("./contracts/ERC20");
const createTokenBridgePrepareSetWethGatewayTransactionRequest_1 = require("./createTokenBridgePrepareSetWethGatewayTransactionRequest");
const createTokenBridgePrepareSetWethGatewayTransactionReceipt_1 = require("./createTokenBridgePrepareSetWethGatewayTransactionReceipt");
const createTokenBridge_1 = require("./createTokenBridge");
const decimals_1 = require("./utils/decimals");
const testnodeAccounts = (0, testHelpers_1.getNitroTestnodePrivateKeyAccounts)();
const l2RollupOwner = testnodeAccounts.l2RollupOwner;
const l3RollupOwner = testnodeAccounts.l3RollupOwner;
const l3TokenBridgeDeployer = testnodeAccounts.l3TokenBridgeDeployer;
const nitroTestnodeL1Client = (0, viem_1.createPublicClient)({
    chain: chains_1.nitroTestnodeL1,
    transport: (0, viem_1.http)(chains_1.nitroTestnodeL1.rpcUrls.default.http[0]),
});
const nitroTestnodeL2Client = (0, viem_1.createPublicClient)({
    chain: chains_1.nitroTestnodeL2,
    transport: (0, viem_1.http)(chains_1.nitroTestnodeL2.rpcUrls.default.http[0]),
});
const nitroTestnodeL3Client = (0, viem_1.createPublicClient)({
    chain: chains_1.nitroTestnodeL3,
    transport: (0, viem_1.http)(chains_1.nitroTestnodeL3.rpcUrls.default.http[0]),
});
function checkTokenBridgeContracts(tokenBridgeContracts) {
    (0, vitest_1.expect)(Object.keys(tokenBridgeContracts)).toHaveLength(2);
    // parent chain contracts
    (0, vitest_1.expect)(Object.keys(tokenBridgeContracts.parentChainContracts)).toHaveLength(6);
    (0, vitest_1.expect)(tokenBridgeContracts.parentChainContracts.router).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.parentChainContracts.standardGateway).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.parentChainContracts.customGateway).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.parentChainContracts.multicall).not.toEqual(viem_1.zeroAddress);
    // orbit chain contracts
    (0, vitest_1.expect)(Object.keys(tokenBridgeContracts.orbitChainContracts)).toHaveLength(9);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.router).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.standardGateway).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.customGateway).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.proxyAdmin).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.beaconProxyFactory).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.upgradeExecutor).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.multicall).not.toEqual(viem_1.zeroAddress);
}
async function checkWethGateways(tokenBridgeContracts, { customFeeToken }) {
    if (customFeeToken) {
        // wethGateway and weth should be the zeroAddress on custom-fee-token chains
        (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.wethGateway).toEqual(viem_1.zeroAddress);
        (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.weth).toEqual(viem_1.zeroAddress);
        (0, vitest_1.expect)(tokenBridgeContracts.parentChainContracts.wethGateway).toEqual(viem_1.zeroAddress);
        (0, vitest_1.expect)(tokenBridgeContracts.parentChainContracts.weth).toEqual(viem_1.zeroAddress);
        return;
    }
    // verify weth gateway (parent chain)
    const registeredWethGatewayOnParentChain = await nitroTestnodeL1Client.readContract({
        address: tokenBridgeContracts.parentChainContracts.router,
        abi: (0, viem_1.parseAbi)(['function l1TokenToGateway(address) view returns (address)']),
        functionName: 'l1TokenToGateway',
        args: [tokenBridgeContracts.parentChainContracts.weth],
    });
    (0, vitest_1.expect)(registeredWethGatewayOnParentChain).toEqual(tokenBridgeContracts.parentChainContracts.wethGateway);
    // verify weth gateway (orbit chain)
    // Note: we pass the address of the token on the parent chain when asking for the registered gateway on the orbit chain
    const registeredWethGatewayOnOrbitChain = await nitroTestnodeL2Client.readContract({
        address: tokenBridgeContracts.orbitChainContracts.router,
        abi: (0, viem_1.parseAbi)(['function l1TokenToGateway(address) view returns (address)']),
        functionName: 'l1TokenToGateway',
        args: [tokenBridgeContracts.parentChainContracts.weth],
    });
    (0, vitest_1.expect)(registeredWethGatewayOnOrbitChain).toEqual(tokenBridgeContracts.orbitChainContracts.wethGateway);
    (0, vitest_1.expect)(tokenBridgeContracts.parentChainContracts.weth).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.parentChainContracts.wethGateway).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.weth).not.toEqual(viem_1.zeroAddress);
    (0, vitest_1.expect)(tokenBridgeContracts.orbitChainContracts.wethGateway).not.toEqual(viem_1.zeroAddress);
}
const nativeTokenDecimals = process.env.INTEGRATION_TEST_DECIMALS
    ? Number(process.env.INTEGRATION_TEST_DECIMALS)
    : 18;
(0, vitest_1.describe)('createTokenBridge utils function', () => {
    (0, vitest_1.it)(`successfully deploys token bridge contracts through token bridge creator`, async () => {
        const testnodeInformation = (0, testHelpers_1.getInformationFromTestnode)();
        // deploy a fresh token bridge creator, because it is only possible to deploy one token bridge per rollup per token bridge creator
        const tokenBridgeCreator = await (0, createTokenBridge_testHelpers_1.deployTokenBridgeCreator)({
            publicClient: nitroTestnodeL1Client,
        });
        const txRequest = await (0, createTokenBridgePrepareTransactionRequest_1.createTokenBridgePrepareTransactionRequest)({
            params: {
                rollup: testnodeInformation.rollup,
                rollupOwner: l2RollupOwner.address,
            },
            parentChainPublicClient: nitroTestnodeL1Client,
            orbitChainPublicClient: nitroTestnodeL2Client,
            account: l2RollupOwner.address,
            gasOverrides: {
                gasLimit: {
                    base: 6000000n,
                },
            },
            retryableGasOverrides: {
                maxGasForFactory: {
                    base: 20000000n,
                },
                maxGasForContracts: {
                    base: 20000000n,
                },
                maxSubmissionCostForFactory: {
                    base: 4000000000000n,
                },
                maxSubmissionCostForContracts: {
                    base: 4000000000000n,
                },
            },
            tokenBridgeCreatorAddressOverride: tokenBridgeCreator,
        });
        // sign and send the transaction
        const txHash = await nitroTestnodeL1Client.sendRawTransaction({
            serializedTransaction: await l2RollupOwner.signTransaction(txRequest),
        });
        // get the transaction receipt after waiting for the transaction to complete
        const txReceipt = (0, createTokenBridgePrepareTransactionReceipt_1.createTokenBridgePrepareTransactionReceipt)(await nitroTestnodeL1Client.waitForTransactionReceipt({ hash: txHash }));
        (0, vitest_1.expect)(txReceipt.status).toEqual('success');
        // checking retryables execution
        const orbitChainRetryableReceipts = await txReceipt.waitForRetryables({
            orbitPublicClient: nitroTestnodeL2Client,
        });
        (0, vitest_1.expect)(orbitChainRetryableReceipts).toHaveLength(2);
        (0, vitest_1.expect)(orbitChainRetryableReceipts[0].status).toEqual('success');
        (0, vitest_1.expect)(orbitChainRetryableReceipts[1].status).toEqual('success');
        // get contracts
        const tokenBridgeContracts = await txReceipt.getTokenBridgeContracts({
            parentChainPublicClient: nitroTestnodeL1Client,
        });
        checkTokenBridgeContracts(tokenBridgeContracts);
        // set weth gateway
        const setWethGatewayTxRequest = await (0, createTokenBridgePrepareSetWethGatewayTransactionRequest_1.createTokenBridgePrepareSetWethGatewayTransactionRequest)({
            rollup: testnodeInformation.rollup,
            parentChainPublicClient: nitroTestnodeL1Client,
            orbitChainPublicClient: nitroTestnodeL2Client,
            account: l2RollupOwner.address,
            retryableGasOverrides: {
                gasLimit: {
                    base: 100000n,
                },
            },
            tokenBridgeCreatorAddressOverride: tokenBridgeCreator,
        });
        // sign and send the transaction
        const setWethGatewayTxHash = await nitroTestnodeL1Client.sendRawTransaction({
            serializedTransaction: await l2RollupOwner.signTransaction(setWethGatewayTxRequest),
        });
        // get the transaction receipt after waiting for the transaction to complete
        const setWethGatewayTxReceipt = (0, createTokenBridgePrepareSetWethGatewayTransactionReceipt_1.createTokenBridgePrepareSetWethGatewayTransactionReceipt)(await nitroTestnodeL1Client.waitForTransactionReceipt({ hash: setWethGatewayTxHash }));
        // checking retryables execution
        const orbitChainSetGatewayRetryableReceipt = await setWethGatewayTxReceipt.waitForRetryables({
            orbitPublicClient: nitroTestnodeL2Client,
        });
        (0, vitest_1.expect)(orbitChainSetGatewayRetryableReceipt).toHaveLength(1);
        (0, vitest_1.expect)(orbitChainSetGatewayRetryableReceipt[0].status).toEqual('success');
        checkWethGateways(tokenBridgeContracts, { customFeeToken: false });
    });
    (0, vitest_1.it)(`successfully deploys token bridge contracts with a custom fee token through token bridge creator`, async () => {
        const testnodeInformation = (0, testHelpers_1.getInformationFromTestnode)();
        // deploy a fresh token bridge creator, because it is only possible to deploy one token bridge per rollup per token bridge creator
        const tokenBridgeCreator = await (0, createTokenBridge_testHelpers_1.deployTokenBridgeCreator)({
            publicClient: nitroTestnodeL2Client,
        });
        // -----------------------------
        // 1. fund l3deployer account
        const fundTxRequestRaw = await nitroTestnodeL2Client.prepareTransactionRequest({
            chain: nitroTestnodeL2Client.chain,
            to: testnodeInformation.l3NativeToken,
            data: (0, viem_1.encodeFunctionData)({
                abi: ERC20_1.erc20ABI,
                functionName: 'transfer',
                args: [
                    l3RollupOwner.address,
                    (0, decimals_1.scaleFrom18DecimalsToNativeTokenDecimals)({ amount: 500n, decimals: nativeTokenDecimals }),
                ],
            }),
            value: BigInt(0),
            account: l3TokenBridgeDeployer,
        });
        // sign and send the transaction
        const fundTxRequest = { ...fundTxRequestRaw, chainId: nitroTestnodeL2Client.chain.id };
        const fundTxHash = await nitroTestnodeL2Client.sendRawTransaction({
            serializedTransaction: await l3TokenBridgeDeployer.signTransaction(fundTxRequest),
        });
        // get the transaction receipt after waiting for the transaction to complete
        const fundTxReceipt = await nitroTestnodeL2Client.waitForTransactionReceipt({
            hash: fundTxHash,
        });
        (0, vitest_1.expect)(fundTxReceipt.status).toEqual('success');
        // -----------------------------
        // 2. approve custom fee token to be spent by the TokenBridgeCreator
        const allowanceParams = {
            nativeToken: testnodeInformation.l3NativeToken,
            owner: l3RollupOwner.address,
            publicClient: nitroTestnodeL2Client,
            tokenBridgeCreatorAddressOverride: tokenBridgeCreator,
        };
        // sign and send the transaction
        const approvalForTokenBridgeCreatorTxHash = await nitroTestnodeL2Client.sendRawTransaction({
            serializedTransaction: await l3RollupOwner.signTransaction(await (0, createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest_1.createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest)(allowanceParams)),
        });
        // get the transaction receipt after waiting for the transaction to complete
        const approvalForNewTokenBridgeCreatorTxReceipt = await nitroTestnodeL2Client.waitForTransactionReceipt({
            hash: approvalForTokenBridgeCreatorTxHash,
        });
        (0, vitest_1.expect)(approvalForNewTokenBridgeCreatorTxReceipt.status).toEqual('success');
        // -----------------------------
        // 3. create the token bridge
        const txRequest = await (0, createTokenBridgePrepareTransactionRequest_1.createTokenBridgePrepareTransactionRequest)({
            params: {
                rollup: testnodeInformation.l3Rollup,
                rollupOwner: l3RollupOwner.address,
            },
            parentChainPublicClient: nitroTestnodeL2Client,
            orbitChainPublicClient: nitroTestnodeL3Client,
            account: l3RollupOwner.address,
            gasOverrides: {
                gasLimit: {
                    base: 6000000n,
                },
            },
            retryableGasOverrides: {
                maxGasForFactory: {
                    base: 20000000n,
                },
                maxGasForContracts: {
                    base: 20000000n,
                },
                maxSubmissionCostForFactory: {
                    base: 4000000000000n,
                },
                maxSubmissionCostForContracts: {
                    base: 4000000000000n,
                },
            },
            tokenBridgeCreatorAddressOverride: tokenBridgeCreator,
        });
        // sign and send the transaction
        const txHash = await nitroTestnodeL2Client.sendRawTransaction({
            serializedTransaction: await l3RollupOwner.signTransaction(txRequest),
        });
        // get the transaction receipt after waiting for the transaction to complete
        const txReceipt = (0, createTokenBridgePrepareTransactionReceipt_1.createTokenBridgePrepareTransactionReceipt)(await nitroTestnodeL2Client.waitForTransactionReceipt({ hash: txHash }));
        (0, vitest_1.expect)(txReceipt.status).toEqual('success');
        // checking retryables execution
        const orbitChainRetryableReceipts = await txReceipt.waitForRetryables({
            orbitPublicClient: nitroTestnodeL3Client,
        });
        (0, vitest_1.expect)(orbitChainRetryableReceipts).toHaveLength(2);
        (0, vitest_1.expect)(orbitChainRetryableReceipts[0].status).toEqual('success');
        (0, vitest_1.expect)(orbitChainRetryableReceipts[1].status).toEqual('success');
        // get contracts
        const tokenBridgeContracts = await txReceipt.getTokenBridgeContracts({
            parentChainPublicClient: nitroTestnodeL2Client,
        });
        checkTokenBridgeContracts(tokenBridgeContracts);
        checkWethGateways(tokenBridgeContracts, { customFeeToken: true });
    });
});
(0, vitest_1.describe)('createTokenBridge', () => {
    (0, vitest_1.it)('successfully deploys token bridge contracts', async () => {
        const testnodeInformation = (0, testHelpers_1.getInformationFromTestnode)();
        // deploy a fresh token bridge creator, because it is only possible to deploy one token bridge per rollup per token bridge creator
        const tokenBridgeCreator = await (0, createTokenBridge_testHelpers_1.deployTokenBridgeCreator)({
            publicClient: nitroTestnodeL1Client,
        });
        const { tokenBridgeContracts } = await (0, createTokenBridge_1.createTokenBridge)({
            rollupOwner: l2RollupOwner.address,
            rollupAddress: testnodeInformation.rollup,
            account: l2RollupOwner,
            parentChainPublicClient: nitroTestnodeL1Client,
            orbitChainPublicClient: nitroTestnodeL2Client,
            tokenBridgeCreatorAddressOverride: tokenBridgeCreator,
            gasOverrides: {
                gasLimit: {
                    base: 6000000n,
                },
            },
            retryableGasOverrides: {
                maxGasForFactory: {
                    base: 20000000n,
                },
                maxGasForContracts: {
                    base: 20000000n,
                },
                maxSubmissionCostForFactory: {
                    base: 4000000000000n,
                },
                maxSubmissionCostForContracts: {
                    base: 4000000000000n,
                },
            },
            setWethGatewayGasOverrides: {
                gasLimit: {
                    base: 100000n,
                },
            },
        });
        checkTokenBridgeContracts(tokenBridgeContracts);
        checkWethGateways(tokenBridgeContracts, { customFeeToken: false });
    });
    (0, vitest_1.it)('successfully deploys token bridge contracts with a custom fee token', async () => {
        const testnodeInformation = (0, testHelpers_1.getInformationFromTestnode)();
        // deploy a fresh token bridge creator, because it is only possible to deploy one token bridge per rollup per token bridge creator
        const tokenBridgeCreator = await (0, createTokenBridge_testHelpers_1.deployTokenBridgeCreator)({
            publicClient: nitroTestnodeL2Client,
        });
        // -----------------------------
        // 1. fund l3deployer account
        const fundTxRequestRaw = await nitroTestnodeL2Client.prepareTransactionRequest({
            chain: nitroTestnodeL2Client.chain,
            to: testnodeInformation.l3NativeToken,
            data: (0, viem_1.encodeFunctionData)({
                abi: ERC20_1.erc20ABI,
                functionName: 'transfer',
                args: [
                    l3RollupOwner.address,
                    (0, decimals_1.scaleFrom18DecimalsToNativeTokenDecimals)({ amount: 500n, decimals: nativeTokenDecimals }),
                ],
            }),
            value: BigInt(0),
            account: l3TokenBridgeDeployer,
        });
        // sign and send the transaction
        const fundTxRequest = { ...fundTxRequestRaw, chainId: nitroTestnodeL2Client.chain.id };
        const fundTxHash = await nitroTestnodeL2Client.sendRawTransaction({
            serializedTransaction: await l3TokenBridgeDeployer.signTransaction(fundTxRequest),
        });
        // get the transaction receipt after waiting for the transaction to complete
        const fundTxReceipt = await nitroTestnodeL2Client.waitForTransactionReceipt({
            hash: fundTxHash,
        });
        (0, vitest_1.expect)(fundTxReceipt.status).toEqual('success');
        // -----------------------------
        // 2. Deploy token bridge contracts
        const { tokenBridgeContracts } = await (0, createTokenBridge_1.createTokenBridge)({
            rollupOwner: l3RollupOwner.address,
            rollupAddress: testnodeInformation.l3Rollup,
            account: l3RollupOwner,
            parentChainPublicClient: nitroTestnodeL2Client,
            orbitChainPublicClient: nitroTestnodeL3Client,
            nativeTokenAddress: testnodeInformation.l3NativeToken,
            tokenBridgeCreatorAddressOverride: tokenBridgeCreator,
            gasOverrides: {
                gasLimit: {
                    base: 6000000n,
                },
            },
            retryableGasOverrides: {
                maxGasForFactory: {
                    base: 20000000n,
                },
                maxGasForContracts: {
                    base: 20000000n,
                },
                maxSubmissionCostForFactory: {
                    base: 4000000000000n,
                },
                maxSubmissionCostForContracts: {
                    base: 4000000000000n,
                },
            },
        });
        checkTokenBridgeContracts(tokenBridgeContracts);
        checkWethGateways(tokenBridgeContracts, { customFeeToken: true });
    });
    (0, vitest_1.it)('should throw when createTokenBridge is called multiple times', async () => {
        const testnodeInformation = (0, testHelpers_1.getInformationFromTestnode)();
        const tokenBridgeCreator = await (0, createTokenBridge_testHelpers_1.deployTokenBridgeCreator)({
            publicClient: nitroTestnodeL1Client,
        });
        const cfg = {
            rollupOwner: l2RollupOwner.address,
            rollupAddress: testnodeInformation.rollup,
            account: l2RollupOwner,
            parentChainPublicClient: nitroTestnodeL1Client,
            orbitChainPublicClient: nitroTestnodeL2Client,
            tokenBridgeCreatorAddressOverride: tokenBridgeCreator,
            gasOverrides: {
                gasLimit: {
                    base: 6000000n,
                },
            },
            retryableGasOverrides: {
                maxGasForFactory: {
                    base: 20000000n,
                },
                maxGasForContracts: {
                    base: 20000000n,
                },
                maxSubmissionCostForFactory: {
                    base: 4000000000000n,
                },
                maxSubmissionCostForContracts: {
                    base: 4000000000000n,
                },
            },
            setWethGatewayGasOverrides: {
                gasLimit: {
                    base: 100000n,
                },
            },
        };
        const { tokenBridgeContracts } = await (0, createTokenBridge_1.createTokenBridge)(cfg);
        await (0, vitest_1.expect)((0, createTokenBridge_1.createTokenBridge)(cfg)).rejects.toThrowError(`Token bridge contracts for Rollup ${testnodeInformation.rollup} are already deployed`);
        checkTokenBridgeContracts(tokenBridgeContracts);
        checkWethGateways(tokenBridgeContracts, { customFeeToken: false });
    });
});

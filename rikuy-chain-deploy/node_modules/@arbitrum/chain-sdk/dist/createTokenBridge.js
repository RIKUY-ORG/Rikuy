"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTokenBridge = void 0;
const createTokenBridgeEnoughCustomFeeTokenAllowance_1 = require("./createTokenBridgeEnoughCustomFeeTokenAllowance");
const createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest_1 = require("./createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest");
const createTokenBridgePrepareTransactionRequest_1 = require("./createTokenBridgePrepareTransactionRequest");
const createTokenBridgePrepareTransactionReceipt_1 = require("./createTokenBridgePrepareTransactionReceipt");
const createTokenBridgePrepareSetWethGatewayTransactionRequest_1 = require("./createTokenBridgePrepareSetWethGatewayTransactionRequest");
const createTokenBridgePrepareSetWethGatewayTransactionReceipt_1 = require("./createTokenBridgePrepareSetWethGatewayTransactionReceipt");
const isNonZeroAddress_1 = require("./utils/isNonZeroAddress");
const getBlockExplorerUrl_1 = require("./utils/getBlockExplorerUrl");
const isTokenBridgeDeployed_1 = require("./isTokenBridgeDeployed");
/**
 * Performs the transactions to deploy the token bridge core contracts
 *
 * For chain with custom gas token, it checks the custom gas token allowance
 * token allowance and approve if necessary.
 *
 * Returns the token bridge core contracts.
 *
 * @param {CreateTokenBridgeParams} createTokenBridgeParams
 * @param {String} createRollupParams.rollupOwner - The address of the rollup owner
 * @param {Object} createRollupParams.rollupAddress - The address of the Rollup contract
 * @param {Object} [createRollupParams.account] - The private key account to sign transactions
 * @param {String} [createRollupParams.nativeTokenAddress=] - Optional
 * If nativeTokenAddress is passed and different than zero address, deploy a token bridge with custom fee token.
 * @param {Object} createRollupParams.parentChainPublicClient - The parent chain Viem Public Client
 * @param {Object} createRollupParams.orbitChainPublicClient - The orbit chain Viem Public Client
 * @param {String} [createRollupParams.tokenBridgeCreatorAddressOverride=] - Optional
 * If tokenBridgeCreatorAddressOverride is passed, the address is overridden in the different transactions
 * @param {Object} [createRollupParams.gasOverrides=] - {@link TransactionRequestGasOverrides} Optional
 * Gas overrides for createTokenBridgePrepareTransactionRequest
 * @param {Object} createRollupParams.retryableGasOverrides - {@link TokenBridgeRetryableGasOverrides} Optional
 * Retryable gas overrides for createTokenBridgePrepareTransactionRequest
 * @param {Object} createRollupParams.setWethGatewayGasOverrides - {@link SetWethGatewayRetryableGasOverrides} Optional
 * Retryable gas overrides for createTokenBridgePrepareSetWethGatewayTransactionRequest
 *
 * @returns Promise<{@link CreateTokenBridgeResults}>
 *
 * @example
 * const tokenBridgeCreator = await deployTokenBridgeCreator({
 *   publicClient: l2Client,
 * });
 *
 * const tokenBridgeContracts = await createTokenBridge({
 *   rollupOwner: rollupOwner.address,
 *   rollupAddress: rollupAddress,
 *   account: deployer,
 *   parentChainPublicClient: l1Client,
 *   orbitChainPublicClient: l2Client,
 *   tokenBridgeCreatorAddressOverride: tokenBridgeCreator,
 *   gasOverrides: {
 *     gasLimit: {
 *       base: 6_000_000n,
 *     },
 *   },
 *   retryableGasOverrides: {
 *     maxGasForFactory: {
 *       base: 20_000_000n,
 *     },
 *     maxGasForContracts: {
 *       base: 20_000_000n,
 *     },
 *     maxSubmissionCostForFactory: {
 *       base: 4_000_000_000_000n,
 *     },
 *     maxSubmissionCostForContracts: {
 *       base: 4_000_000_000_000n,
 *     },
 *   },
 *   setWethGatewayGasOverrides: {
 *     gasLimit: {
 *       base: 100_000n,
 *     },
 *   },
 * });
 */
async function createTokenBridge({ rollupOwner, rollupAddress, rollupDeploymentBlockNumber, account, nativeTokenAddress, parentChainPublicClient, orbitChainPublicClient, tokenBridgeCreatorAddressOverride, gasOverrides, retryableGasOverrides, setWethGatewayGasOverrides, }) {
    const isTokenBridgeAlreadyDeployed = await (0, isTokenBridgeDeployed_1.isTokenBridgeDeployed)({
        parentChainPublicClient,
        orbitChainPublicClient,
        rollup: rollupAddress,
        tokenBridgeCreatorAddressOverride,
    });
    if (isTokenBridgeAlreadyDeployed) {
        throw new Error(`Token bridge contracts for Rollup ${rollupAddress} are already deployed`);
    }
    const isCustomFeeTokenBridge = (0, isNonZeroAddress_1.isNonZeroAddress)(nativeTokenAddress);
    if (isCustomFeeTokenBridge) {
        // set the custom fee token
        // prepare transaction to approve custom fee token spend
        const allowanceParams = {
            nativeToken: nativeTokenAddress,
            owner: account.address,
            publicClient: parentChainPublicClient,
            tokenBridgeCreatorAddressOverride,
        };
        // Check allowance and approve if necessary
        if (!(await (0, createTokenBridgeEnoughCustomFeeTokenAllowance_1.createTokenBridgeEnoughCustomFeeTokenAllowance)(allowanceParams))) {
            const approvalTxRequest = await (0, createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest_1.createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest)(allowanceParams);
            // sign and send the transaction
            const approvalTxHash = await parentChainPublicClient.sendRawTransaction({
                serializedTransaction: await account.signTransaction(approvalTxRequest),
            });
            // get the transaction receipt after waiting for the transaction to complete
            const approvalTxReceipt = await parentChainPublicClient.waitForTransactionReceipt({
                hash: approvalTxHash,
            });
            console.log(`Tokens approved in ${(0, getBlockExplorerUrl_1.getBlockExplorerUrl)(parentChainPublicClient.chain)}/tx/${approvalTxReceipt.transactionHash}`);
        }
    }
    // prepare the transaction for deploying the core contracts
    const txRequest = await (0, createTokenBridgePrepareTransactionRequest_1.createTokenBridgePrepareTransactionRequest)({
        params: {
            rollup: rollupAddress,
            rollupOwner,
        },
        parentChainPublicClient,
        orbitChainPublicClient,
        account: account.address,
        tokenBridgeCreatorAddressOverride,
        gasOverrides,
        retryableGasOverrides,
    });
    // sign and send the transaction
    console.log(`Deploying the TokenBridge...`);
    const txHash = await parentChainPublicClient.sendRawTransaction({
        serializedTransaction: await account.signTransaction(txRequest),
    });
    const transaction = await parentChainPublicClient.getTransaction({ hash: txHash });
    // get the transaction receipt after waiting for the transaction to complete
    const txReceipt = (0, createTokenBridgePrepareTransactionReceipt_1.createTokenBridgePrepareTransactionReceipt)(await parentChainPublicClient.waitForTransactionReceipt({ hash: txHash }));
    console.log(`Deployed in ${(0, getBlockExplorerUrl_1.getBlockExplorerUrl)(parentChainPublicClient.chain)}/tx/${txReceipt.transactionHash}`);
    // wait for retryables to execute
    console.log(`Waiting for retryable tickets to execute on the Orbit chain...`);
    const orbitChainRetryableReceipts = await txReceipt.waitForRetryables({
        // @ts-ignore (todo: fix viem type issue)
        orbitPublicClient: orbitChainPublicClient,
    });
    console.log(`Retryables executed`);
    console.log(`Transaction hash for first retryable is ${orbitChainRetryableReceipts[0].transactionHash}`);
    console.log(`Transaction hash for second retryable is ${orbitChainRetryableReceipts[1].transactionHash}`);
    // fetching the TokenBridge contracts
    const tokenBridgeContracts = await txReceipt.getTokenBridgeContracts({
        // @ts-ignore (todo: fix viem type issue)
        parentChainPublicClient,
    });
    // Non custom fee token
    if (!isCustomFeeTokenBridge) {
        // set weth gateway
        const setWethGatewayTxRequest = await (0, createTokenBridgePrepareSetWethGatewayTransactionRequest_1.createTokenBridgePrepareSetWethGatewayTransactionRequest)({
            rollup: rollupAddress,
            rollupDeploymentBlockNumber,
            parentChainPublicClient,
            orbitChainPublicClient,
            account: account.address,
            tokenBridgeCreatorAddressOverride,
            retryableGasOverrides: setWethGatewayGasOverrides,
        });
        // sign and send the transaction
        const setWethGatewayTxHash = await parentChainPublicClient.sendRawTransaction({
            serializedTransaction: await account.signTransaction(setWethGatewayTxRequest),
        });
        const setWethGatewayTransaction = await parentChainPublicClient.getTransaction({
            hash: setWethGatewayTxHash,
        });
        // get the transaction receipt after waiting for the transaction to complete
        const setWethGatewayTxReceipt = (0, createTokenBridgePrepareSetWethGatewayTransactionReceipt_1.createTokenBridgePrepareSetWethGatewayTransactionReceipt)(await parentChainPublicClient.waitForTransactionReceipt({ hash: setWethGatewayTxHash }));
        console.log(`Weth gateway set in ${(0, getBlockExplorerUrl_1.getBlockExplorerUrl)(parentChainPublicClient.chain)}/tx/${setWethGatewayTxReceipt.transactionHash}`);
        // Wait for retryables to execute
        const orbitChainSetWethGatewayRetryableReceipt = await setWethGatewayTxReceipt.waitForRetryables({
            // @ts-ignore (todo: fix viem type issue)
            orbitPublicClient: orbitChainPublicClient,
        });
        console.log(`Retryables executed`);
        console.log(`Transaction hash for retryable is ${orbitChainSetWethGatewayRetryableReceipt[0].transactionHash}`);
        if (orbitChainSetWethGatewayRetryableReceipt[0].status !== 'success') {
            throw new Error(`Retryable status is not success: ${orbitChainSetWethGatewayRetryableReceipt[0].status}. Aborting...`);
        }
        return {
            transaction,
            // @ts-ignore (todo: fix viem type issue)
            transactionReceipt: txReceipt,
            retryables: orbitChainRetryableReceipts,
            tokenBridgeContracts,
            setWethGateway: {
                transaction: setWethGatewayTransaction,
                // @ts-ignore (todo: fix viem type issue)
                transactionReceipt: setWethGatewayTxReceipt,
                retryables: [orbitChainSetWethGatewayRetryableReceipt[0]],
            },
        };
    }
    return {
        transaction,
        // @ts-ignore (todo: fix viem type issue)
        transactionReceipt: txReceipt,
        retryables: orbitChainRetryableReceipts,
        tokenBridgeContracts,
    };
}
exports.createTokenBridge = createTokenBridge;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const viem_1 = require("viem");
const accounts_1 = require("viem/accounts");
const chains_1 = require("../chains");
const rollupAdminLogicPublicActions_1 = require("./rollupAdminLogicPublicActions");
const testHelpers_1 = require("../testHelpers");
const getValidators_1 = require("../getValidators");
const { l3RollupOwner } = (0, testHelpers_1.getNitroTestnodePrivateKeyAccounts)();
const { l3Rollup, l3UpgradeExecutor } = (0, testHelpers_1.getInformationFromTestnode)();
const rollupCreatorVersion = (0, testHelpers_1.testHelper_getRollupCreatorVersionFromEnv)();
// https://github.com/OffchainLabs/nitro-testnode/blob/release/test-node.bash#L634
// https://github.com/OffchainLabs/nitro-contracts/blob/v3.1.1/scripts/rollupCreation.ts#L250-L257
// https://github.com/OffchainLabs/nitro-contracts/blob/v2.1.3/scripts/rollupCreation.ts#L237-L243
const expectedInitialValidators = rollupCreatorVersion === 'v3.1' ? 11 : 10;
const client = (0, viem_1.createPublicClient)({
    chain: chains_1.nitroTestnodeL2,
    transport: (0, viem_1.http)(),
}).extend((0, rollupAdminLogicPublicActions_1.rollupAdminLogicPublicActions)({
    rollup: l3Rollup,
}));
(0, vitest_1.it)('successfully set validators', async () => {
    const randomAccounts = [
        (0, accounts_1.privateKeyToAccount)((0, accounts_1.generatePrivateKey)()).address,
        (0, accounts_1.privateKeyToAccount)((0, accounts_1.generatePrivateKey)()).address,
    ];
    const { validators: initialValidators, isAccurate: isAccurateInitially } = await (0, getValidators_1.getValidators)(client, {
        rollup: l3Rollup,
    });
    (0, vitest_1.expect)(initialValidators).toHaveLength(expectedInitialValidators);
    (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
    const tx = await client.rollupAdminLogicPrepareTransactionRequest({
        functionName: 'setValidator',
        args: [randomAccounts, [true, false]],
        account: l3RollupOwner.address,
        upgradeExecutor: l3UpgradeExecutor,
        rollup: l3Rollup,
    });
    const txHash = await client.sendRawTransaction({
        serializedTransaction: await l3RollupOwner.signTransaction(tx),
    });
    await client.waitForTransactionReceipt({
        hash: txHash,
    });
    const validators = await Promise.all([
        client.rollupAdminLogicReadContract({
            functionName: 'isValidator',
            args: [randomAccounts[0]],
            rollup: l3Rollup,
        }),
        client.rollupAdminLogicReadContract({
            functionName: 'isValidator',
            args: [randomAccounts[1]],
            rollup: l3Rollup,
        }),
    ]);
    const { validators: currentValidators, isAccurate: currentIsAccurate } = await (0, getValidators_1.getValidators)(client, { rollup: l3Rollup });
    (0, vitest_1.expect)(validators).toEqual([true, false]);
    (0, vitest_1.expect)(currentValidators).toEqual(initialValidators.concat(randomAccounts[0]));
    (0, vitest_1.expect)(currentIsAccurate).toBeTruthy();
    const revertTx = await client.rollupAdminLogicPrepareTransactionRequest({
        functionName: 'setValidator',
        args: [randomAccounts, [false, false]],
        account: l3RollupOwner.address,
        upgradeExecutor: l3UpgradeExecutor,
        rollup: l3Rollup,
    });
    const revertTxHash = await client.sendRawTransaction({
        serializedTransaction: await l3RollupOwner.signTransaction(revertTx),
    });
    await client.waitForTransactionReceipt({
        hash: revertTxHash,
    });
    const { validators: revertedValidators, isAccurate: revertedIsAccurate } = await (0, getValidators_1.getValidators)(client, {
        rollup: l3Rollup,
    });
    (0, vitest_1.expect)(revertedValidators).toEqual(initialValidators);
    (0, vitest_1.expect)(revertedIsAccurate).toBeTruthy();
});
(0, vitest_1.it)('successfully enable/disable whitelist', async () => {
    const whitelistDisabledBefore = await client.rollupAdminLogicReadContract({
        functionName: 'validatorWhitelistDisabled',
    });
    // By default whitelist is not disabled
    (0, vitest_1.expect)(whitelistDisabledBefore).toEqual(false);
    const tx = await client.rollupAdminLogicPrepareTransactionRequest({
        functionName: 'setValidatorWhitelistDisabled',
        args: [true],
        account: l3RollupOwner.address,
        rollup: l3Rollup,
        upgradeExecutor: l3UpgradeExecutor,
    });
    const txHash = await client.sendRawTransaction({
        serializedTransaction: await l3RollupOwner.signTransaction(tx),
    });
    await client.waitForTransactionReceipt({
        hash: txHash,
    });
    const whitelistDisabled = await client.rollupAdminLogicReadContract({
        functionName: 'validatorWhitelistDisabled',
        rollup: l3Rollup,
    });
    (0, vitest_1.expect)(whitelistDisabled).toEqual(true);
    // Revert changes, so test can be run multiple time without issues
    const revertTx = await client.rollupAdminLogicPrepareTransactionRequest({
        functionName: 'setValidatorWhitelistDisabled',
        args: [false],
        account: l3RollupOwner.address,
        rollup: l3Rollup,
        upgradeExecutor: l3UpgradeExecutor,
    });
    const revertTxHash = await client.sendRawTransaction({
        serializedTransaction: await l3RollupOwner.signTransaction(revertTx),
    });
    await client.waitForTransactionReceipt({
        hash: revertTxHash,
    });
});

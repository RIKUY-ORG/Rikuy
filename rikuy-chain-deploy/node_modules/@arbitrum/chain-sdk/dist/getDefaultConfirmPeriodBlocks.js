"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDefaultConfirmPeriodBlocks = void 0;
const ParentChain_1 = require("./types/ParentChain");
const parentChainIsMainnet_1 = require("./parentChainIsMainnet");
const getParentChainBlockTime_1 = require("./getParentChainBlockTime");
function getDefaultConfirmPeriodBlocks(parentChainIdOrClient) {
    const { chainId: parentChainId, isCustom: parentChainIsCustom } = (0, ParentChain_1.validateParentChain)(parentChainIdOrClient);
    if (parentChainIsCustom) {
        throw new Error(`[getDefaultConfirmPeriodBlocks] can't provide defaults for custom parent chain with id ${parentChainId}`);
    }
    const isMainnet = (0, parentChainIsMainnet_1.parentChainIsMainnet)(parentChainId);
    const blocksPerMinute = 60 / (0, getParentChainBlockTime_1.getParentChainBlockTime)(parentChainId);
    return isMainnet
        ? // 7 days
            BigInt(7 * 24 * 60 * blocksPerMinute)
        : // 30 minutes
            BigInt(30 * blocksPerMinute);
}
exports.getDefaultConfirmPeriodBlocks = getDefaultConfirmPeriodBlocks;

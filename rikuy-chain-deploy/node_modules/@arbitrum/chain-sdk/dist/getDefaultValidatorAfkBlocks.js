"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getDefaultValidatorAfkBlocks = void 0;
const ParentChain_1 = require("./types/ParentChain");
const getParentChainBlockTime_1 = require("./getParentChainBlockTime");
function getDefaultValidatorAfkBlocks(parentChainIdOrClient) {
    const { chainId: parentChainId, isCustom: parentChainIsCustom } = (0, ParentChain_1.validateParentChain)(parentChainIdOrClient);
    if (parentChainIsCustom) {
        throw new Error(`[getDefaultValidatorAfkBlocks] can't provide defaults for custom parent chain with id ${parentChainId}`);
    }
    const blocksPerMinute = 60 / (0, getParentChainBlockTime_1.getParentChainBlockTime)(parentChainId);
    // https://github.com/OffchainLabs/nitro-contracts/blob/main/src/rollup/RollupAdminLogic.sol#L58-L60
    //
    // 28 days
    //
    // Since it can take 14 days under normal circumstances to confirm an assertion, this means
    // the validators will have been inactive for a further 14 days before the whitelist is removed.
    return BigInt(28 * 24 * 60 * blocksPerMinute);
}
exports.getDefaultValidatorAfkBlocks = getDefaultValidatorAfkBlocks;

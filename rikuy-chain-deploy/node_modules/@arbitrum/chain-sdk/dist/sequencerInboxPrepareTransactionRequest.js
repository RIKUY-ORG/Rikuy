"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sequencerInboxPrepareTransactionRequest = exports.sequencerInboxPrepareFunctionData = void 0;
const viem_1 = require("viem");
const SequencerInbox_1 = require("./contracts/SequencerInbox");
const upgradeExecutorEncodeFunctionData_1 = require("./upgradeExecutorEncodeFunctionData");
const ParentChain_1 = require("./types/ParentChain");
function sequencerInboxEncodeFunctionData({ abi, functionName, args, }) {
    return (0, viem_1.encodeFunctionData)({
        abi,
        functionName,
        args,
    });
}
function sequencerInboxPrepareFunctionData(params) {
    const { upgradeExecutor } = params;
    if (!upgradeExecutor) {
        return {
            to: params.sequencerInbox,
            data: sequencerInboxEncodeFunctionData(params),
            value: BigInt(0),
        };
    }
    return {
        to: upgradeExecutor,
        data: (0, upgradeExecutorEncodeFunctionData_1.upgradeExecutorEncodeFunctionData)({
            functionName: 'executeCall',
            args: [
                params.sequencerInbox,
                sequencerInboxEncodeFunctionData(params), // targetCallData
            ],
        }),
        value: BigInt(0),
    };
}
exports.sequencerInboxPrepareFunctionData = sequencerInboxPrepareFunctionData;
async function sequencerInboxPrepareTransactionRequest(client, params) {
    const { chainId } = (0, ParentChain_1.validateParentChain)(client);
    // params is extending SequencerInboxPrepareFunctionDataParameters, it's safe to cast
    const { to, data, value } = sequencerInboxPrepareFunctionData({
        ...params,
        abi: SequencerInbox_1.sequencerInboxABI,
    });
    // @ts-ignore (todo: fix viem type issue)
    const request = await client.prepareTransactionRequest({
        chain: client.chain,
        to,
        data,
        value,
        account: params.account,
    });
    return { ...request, chainId };
}
exports.sequencerInboxPrepareTransactionRequest = sequencerInboxPrepareTransactionRequest;

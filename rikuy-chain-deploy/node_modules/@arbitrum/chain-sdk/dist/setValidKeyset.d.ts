import { PublicClient, Transport, Chain, WalletClient } from 'viem';
import { CoreContracts } from './types/CoreContracts';
export type SetValidKeysetParams<TChain extends Chain | undefined> = {
    coreContracts: Pick<CoreContracts, 'upgradeExecutor' | 'sequencerInbox'>;
    keyset: `0x${string}`;
    publicClient: PublicClient<Transport, TChain>;
    walletClient: WalletClient;
};
export declare function setValidKeyset<TChain extends Chain | undefined>({ coreContracts, keyset, publicClient, walletClient, }: SetValidKeysetParams<TChain>): Promise<import("viem").ExtractChainFormatterReturnType<TChain, "transactionReceipt", import("viem").TransactionReceipt>>;

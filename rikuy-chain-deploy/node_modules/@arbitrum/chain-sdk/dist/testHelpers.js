"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.testHelper_getRollupCreatorVersionFromEnv = exports.testHelper_createCustomParentChain = exports.createRollupHelper = exports.getInformationFromTestnode = exports.getNitroTestnodePrivateKeyAccounts = void 0;
const viem_1 = require("viem");
const accounts_1 = require("viem/accounts");
const dotenv_1 = require("dotenv");
const node_child_process_1 = require("node:child_process");
const utils_1 = require("./utils");
const createRollup_1 = require("./createRollup");
const createRollupPrepareDeploymentParamsConfig_1 = require("./createRollupPrepareDeploymentParamsConfig");
const prepareChainConfig_1 = require("./prepareChainConfig");
(0, dotenv_1.config)();
function getNitroTestnodePrivateKeyAccounts() {
    if (typeof process.env.NITRO_TESTNODE_DEPLOYER_PRIVATE_KEY === 'undefined' ||
        typeof process.env.NITRO_TESTNODE_L2_ROLLUP_OWNER_PRIVATE_KEY === 'undefined' ||
        typeof process.env.NITRO_TESTNODE_L3_ROLLUP_OWNER_PRIVATE_KEY === 'undefined' ||
        typeof process.env.NITRO_TESTNODE_L2_TOKEN_BRIDGE_DEPLOYER_PRIVATE_KEY === 'undefined' ||
        typeof process.env.NITRO_TESTNODE_L3_TOKEN_BRIDGE_DEPLOYER_PRIVATE_KEY === 'undefined') {
        throw Error(`required env variables: NITRO_TESTNODE_DEPLOYER_PRIVATE_KEY, NITRO_TESTNODE_L2_ROLLUP_OWNER_PRIVATE_KEY,
      NITRO_TESTNODE_L2_TOKEN_BRIDGE_DEPLOYER_PRIVATE_KEY, NITRO_TESTNODE_L3_ROLLUP_OWNER_PRIVATE_KEY,
      NITRO_TESTNODE_L3_TOKEN_BRIDGE_DEPLOYER_PRIVATE_KEY`);
    }
    const deployerPrivateKey = (0, utils_1.sanitizePrivateKey)(process.env.NITRO_TESTNODE_DEPLOYER_PRIVATE_KEY);
    const l2RollupOwnerPrivateKey = (0, utils_1.sanitizePrivateKey)(process.env.NITRO_TESTNODE_L2_ROLLUP_OWNER_PRIVATE_KEY);
    const l2TokenBridgeDeployerPrivateKey = (0, utils_1.sanitizePrivateKey)(process.env.NITRO_TESTNODE_L2_TOKEN_BRIDGE_DEPLOYER_PRIVATE_KEY);
    const l3RollupOwnerPrivateKey = (0, utils_1.sanitizePrivateKey)(process.env.NITRO_TESTNODE_L3_ROLLUP_OWNER_PRIVATE_KEY);
    const l3TokenBridgeDeployerPrivateKey = (0, utils_1.sanitizePrivateKey)(process.env.NITRO_TESTNODE_L3_TOKEN_BRIDGE_DEPLOYER_PRIVATE_KEY);
    return {
        deployer: { ...(0, accounts_1.privateKeyToAccount)(deployerPrivateKey), privateKey: deployerPrivateKey },
        l2RollupOwner: {
            ...(0, accounts_1.privateKeyToAccount)(l2RollupOwnerPrivateKey),
            privateKey: l2RollupOwnerPrivateKey,
        },
        l2TokenBridgeDeployer: {
            ...(0, accounts_1.privateKeyToAccount)(l2TokenBridgeDeployerPrivateKey),
            privateKey: l2TokenBridgeDeployerPrivateKey,
        },
        l3RollupOwner: {
            ...(0, accounts_1.privateKeyToAccount)(l3RollupOwnerPrivateKey),
            privateKey: l3RollupOwnerPrivateKey,
        },
        l3TokenBridgeDeployer: {
            ...(0, accounts_1.privateKeyToAccount)(l3TokenBridgeDeployerPrivateKey),
            privateKey: l3TokenBridgeDeployerPrivateKey,
        },
    };
}
exports.getNitroTestnodePrivateKeyAccounts = getNitroTestnodePrivateKeyAccounts;
function getInformationFromTestnode() {
    const containers = [
        'nitro_sequencer_1',
        'nitro-sequencer-1',
        'nitro-testnode-sequencer-1',
        'nitro-testnode_sequencer_1',
    ];
    for (const container of containers) {
        try {
            const deploymentJson = JSON.parse((0, node_child_process_1.execSync)('docker exec ' + container + ' cat /config/deployment.json').toString());
            const l3DeploymentJson = JSON.parse((0, node_child_process_1.execSync)('docker exec ' + container + ' cat /config/l3deployment.json').toString());
            const sequencerConfig = JSON.parse((0, node_child_process_1.execSync)('docker exec ' + container + ' cat /config/sequencer_config.json').toString());
            const l3SequencerConfig = JSON.parse((0, node_child_process_1.execSync)('docker exec ' + container + ' cat /config/l3node_config.json').toString());
            return {
                bridge: deploymentJson['bridge'],
                rollup: deploymentJson['rollup'],
                sequencerInbox: deploymentJson['sequencer-inbox'],
                batchPoster: sequencerConfig.node['batch-poster']['parent-chain-wallet'].account,
                l3Bridge: l3DeploymentJson['bridge'],
                l3Rollup: l3DeploymentJson['rollup'],
                l3SequencerInbox: l3DeploymentJson['sequencer-inbox'],
                l3NativeToken: l3DeploymentJson['native-token'],
                l3BatchPoster: l3SequencerConfig.node['batch-poster']['parent-chain-wallet'].account,
                l3UpgradeExecutor: l3DeploymentJson['upgrade-executor'],
            };
        }
        catch {
            // empty on purpose
        }
    }
    throw new Error('nitro-testnode sequencer not found');
}
exports.getInformationFromTestnode = getInformationFromTestnode;
async function createRollupHelper({ deployer, batchPosters, validators, nativeToken = viem_1.zeroAddress, client, rollupCreatorVersion = testHelper_getRollupCreatorVersionFromEnv(), }) {
    const chainId = (0, utils_1.generateChainId)();
    const createRollupConfig = (0, createRollupPrepareDeploymentParamsConfig_1.createRollupPrepareDeploymentParamsConfig)(client, {
        chainId: BigInt(chainId),
        owner: deployer.address,
        chainConfig: (0, prepareChainConfig_1.prepareChainConfig)({
            chainId,
            arbitrum: {
                InitialChainOwner: deployer.address,
                DataAvailabilityCommittee: true,
            },
        }),
    }, rollupCreatorVersion);
    const createRollupInformation = rollupCreatorVersion === 'v2.1'
        ? await (0, createRollup_1.createRollup)({
            params: {
                config: createRollupConfig,
                batchPosters,
                validators,
                nativeToken,
            },
            account: deployer,
            parentChainPublicClient: client,
            rollupCreatorVersion: 'v2.1',
        })
        : await (0, createRollup_1.createRollup)({
            params: {
                config: createRollupConfig,
                batchPosters,
                validators,
                nativeToken,
            },
            account: deployer,
            parentChainPublicClient: client,
            rollupCreatorVersion: 'v3.1',
        });
    // create test rollup with ETH as gas token
    return {
        createRollupConfig,
        createRollupInformation,
    };
}
exports.createRollupHelper = createRollupHelper;
function testHelper_createCustomParentChain(params) {
    const chainId = params?.id ?? (0, utils_1.generateChainId)();
    const rollupCreator = (0, accounts_1.privateKeyToAccount)((0, accounts_1.generatePrivateKey)()).address;
    const tokenBridgeCreator = (0, accounts_1.privateKeyToAccount)((0, accounts_1.generatePrivateKey)()).address;
    // randomly generated and hardcoded because it must not change for snapshot tests
    const weth = '0xabaF3D9f5cbDcE54cDc43b429d8f7154A3E19Afa';
    return {
        id: chainId,
        name: `Custom Parent Chain (${chainId})`,
        network: `custom-parent-chain-${chainId}`,
        nativeCurrency: {
            name: 'Ether',
            symbol: 'ETH',
            decimals: 18,
        },
        rpcUrls: {
            public: {
                // have to put a valid rpc here so using arbitrum sepolia
                http: ['https://sepolia-rollup.arbitrum.io/rpc'],
            },
            default: {
                // have to put a valid rpc here so using arbitrum sepolia
                http: ['https://sepolia-rollup.arbitrum.io/rpc'],
            },
        },
        contracts: {
            rollupCreator: { address: rollupCreator },
            tokenBridgeCreator: { address: tokenBridgeCreator },
            weth: { address: weth },
        },
    };
}
exports.testHelper_createCustomParentChain = testHelper_createCustomParentChain;
function testHelper_getRollupCreatorVersionFromEnv() {
    if (process.env.INTEGRATION_TEST_NITRO_CONTRACTS_BRANCH) {
        // extract just major and minor version numbers
        return process.env.INTEGRATION_TEST_NITRO_CONTRACTS_BRANCH.split('.')
            .slice(0, 2)
            .join('.');
    }
    return 'v3.1';
}
exports.testHelper_getRollupCreatorVersionFromEnv = testHelper_getRollupCreatorVersionFromEnv;

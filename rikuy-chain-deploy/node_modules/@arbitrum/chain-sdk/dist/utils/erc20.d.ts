import { Address, PublicClient, Transport, Chain, WalletClient } from 'viem';
export type ApprovePrepareTransactionRequestProps<TChain extends Chain | undefined> = {
    address: Address;
    owner: Address;
    spender: Address;
    amount: bigint;
    publicClient: PublicClient<Transport, TChain>;
};
export declare function approvePrepareTransactionRequest<TChain extends Chain | undefined>({ address, owner, spender, amount, publicClient, }: ApprovePrepareTransactionRequestProps<TChain>): Promise<import("viem").PrepareTransactionRequestReturnType>;
export type ApproveProps<TChain extends Chain | undefined> = {
    address: Address;
    spender: Address;
    amount: bigint;
    publicClient: PublicClient<Transport, TChain>;
    walletClient: WalletClient;
};
export declare function approve<TChain extends Chain | undefined>({ address, spender, amount, publicClient, walletClient, }: ApproveProps<TChain>): Promise<import("viem").ExtractChainFormatterReturnType<TChain, "transactionReceipt", import("viem").TransactionReceipt>>;
export type FetchAllowanceProps<TChain extends Chain | undefined> = {
    address: Address;
    owner: Address;
    spender: Address;
    publicClient: PublicClient<Transport, TChain>;
};
export declare function fetchAllowance<TChain extends Chain | undefined>({ address, owner, spender, publicClient, }: FetchAllowanceProps<TChain>): Promise<bigint>;
export type FetchDecimalsProps<TChain extends Chain | undefined> = {
    address: Address;
    publicClient: PublicClient<Transport, TChain>;
};
export declare function fetchDecimals<TChain extends Chain | undefined>({ address, publicClient, }: FetchDecimalsProps<TChain>): Promise<number>;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getClientVersion = void 0;
async function getClientVersion(clientOrRpcUrl) {
    const rpcUrl = typeof clientOrRpcUrl === 'string'
        ? clientOrRpcUrl
        : clientOrRpcUrl.chain?.rpcUrls.default.http[0];
    if (typeof rpcUrl === 'undefined') {
        throw new Error(`[getClientVersion] invalid rpc url: ${rpcUrl}`);
    }
    const response = await fetch(rpcUrl, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            jsonrpc: '2.0',
            method: 'web3_clientVersion',
            params: [],
            id: 1,
        }),
    });
    return (await response.json()).result;
}
exports.getClientVersion = getClientVersion;

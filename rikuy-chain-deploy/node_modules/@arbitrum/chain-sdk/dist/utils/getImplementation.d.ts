import { Chain, Transport, PublicClient, Address } from 'viem';
export declare function getImplementation<TChain extends Chain | undefined>({ client, address, }: {
    client: PublicClient<Transport, TChain>;
    address: Address;
}): Promise<Address>;

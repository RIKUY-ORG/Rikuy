import { Address, PublicClient, Transport, Chain } from 'viem';
export declare function isCustomFeeTokenChain<TChain extends Chain | undefined>({ rollup, parentChainPublicClient, }: {
    rollup: Address;
    parentChainPublicClient: PublicClient<Transport, TChain>;
}): Promise<boolean>;

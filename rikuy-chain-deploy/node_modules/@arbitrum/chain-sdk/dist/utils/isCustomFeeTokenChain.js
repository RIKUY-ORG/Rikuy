"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isCustomFeeTokenChain = void 0;
const viem_1 = require("viem");
async function isCustomFeeTokenChain({ rollup, parentChainPublicClient, }) {
    const bridge = await parentChainPublicClient.readContract({
        address: rollup,
        abi: (0, viem_1.parseAbi)(['function bridge() view returns (address)']),
        functionName: 'bridge',
    });
    try {
        await parentChainPublicClient.readContract({
            address: bridge,
            abi: (0, viem_1.parseAbi)(['function nativeToken() view returns (address)']),
            functionName: 'nativeToken',
        });
    }
    catch {
        return false;
    }
    return true;
}
exports.isCustomFeeTokenChain = isCustomFeeTokenChain;

import { JsonRpcProvider } from '@ethersproject/providers';
import { ArbitrumNetwork } from '@arbitrum/sdk';
import { Address, Chain, PublicClient, Transport } from 'viem';
export declare function prepareArbitrumNetwork<TChain extends Chain | undefined>(parentChainPublicClient: PublicClient<Transport, TChain>, { rollup }: {
    rollup: Address;
}): Promise<ArbitrumNetwork>;
export declare const registerNewNetwork: (parentProvider: JsonRpcProvider, childProvider: JsonRpcProvider, rollupAddress: string) => Promise<ArbitrumNetwork>;

import { Client, Transport, Chain } from 'viem';
import { chains } from '../chains';
type ChainId = (typeof chains)[number]['id'];
export declare function validateChain<TChain extends Chain | undefined>(chainIdOrClient: number | Client<Transport, TChain>): ChainId;
export {};

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateChain = void 0;
const chains_1 = require("../chains");
function isValidChainId(chainId) {
    const ids = chains_1.chains.map((chain) => chain.id);
    return ids.includes(Number(chainId));
}
function validateChain(chainIdOrClient) {
    const chainId = typeof chainIdOrClient === 'number' ? chainIdOrClient : chainIdOrClient.chain?.id;
    if (!isValidChainId(chainId)) {
        throw new Error(`Chain not supported: ${chainId}`);
    }
    return chainId;
}
exports.validateChain = validateChain;

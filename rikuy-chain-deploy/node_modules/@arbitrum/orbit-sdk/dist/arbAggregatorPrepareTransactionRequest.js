"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.arbAggregatorPrepareTransactionRequest = exports.arbAggregatorPrepareFunctionData = void 0;
const viem_1 = require("viem");
const ArbAggregator_1 = require("./contracts/ArbAggregator");
const upgradeExecutorEncodeFunctionData_1 = require("./upgradeExecutorEncodeFunctionData");
function arbAggregatorEncodeFunctionData({ functionName, abi, args }) {
    return (0, viem_1.encodeFunctionData)({
        abi,
        functionName,
        args,
    });
}
function arbAggregatorPrepareFunctionData(params) {
    const { upgradeExecutor } = params;
    if (!upgradeExecutor) {
        return {
            to: ArbAggregator_1.arbAggregatorAddress,
            data: arbAggregatorEncodeFunctionData(params),
            value: BigInt(0),
        };
    }
    return {
        to: upgradeExecutor,
        data: (0, upgradeExecutorEncodeFunctionData_1.upgradeExecutorEncodeFunctionData)({
            functionName: 'executeCall',
            args: [
                ArbAggregator_1.arbAggregatorAddress,
                arbAggregatorEncodeFunctionData(params), // targetCallData
            ],
        }),
        value: BigInt(0),
    };
}
exports.arbAggregatorPrepareFunctionData = arbAggregatorPrepareFunctionData;
async function arbAggregatorPrepareTransactionRequest(client, params) {
    if (typeof client.chain === 'undefined') {
        throw new Error('[arbAggregatorPrepareTransactionRequest] client.chain is undefined');
    }
    // params is extending ArbAggregatorPrepareFunctionDataParameters, it's safe to cast
    const { to, data, value } = arbAggregatorPrepareFunctionData({
        ...params,
        abi: ArbAggregator_1.arbAggregatorABI,
    });
    // @ts-ignore (todo: fix viem type issue)
    const request = await client.prepareTransactionRequest({
        chain: client.chain,
        to,
        data,
        value,
        account: params.account,
    });
    return { ...request, chainId: client.chain.id };
}
exports.arbAggregatorPrepareTransactionRequest = arbAggregatorPrepareTransactionRequest;

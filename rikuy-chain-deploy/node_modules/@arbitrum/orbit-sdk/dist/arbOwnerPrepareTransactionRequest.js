"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.arbOwnerPrepareTransactionRequest = exports.arbOwnerPrepareFunctionData = void 0;
const viem_1 = require("viem");
const ArbOwner_1 = require("./contracts/ArbOwner");
const upgradeExecutorEncodeFunctionData_1 = require("./upgradeExecutorEncodeFunctionData");
const gasOverrides_1 = require("./utils/gasOverrides");
function arbOwnerEncodeFunctionData({ functionName, abi, args }) {
    return (0, viem_1.encodeFunctionData)({
        abi,
        functionName,
        args,
    });
}
function arbOwnerPrepareFunctionData(params) {
    const { upgradeExecutor } = params;
    if (!upgradeExecutor) {
        return {
            to: ArbOwner_1.arbOwnerAddress,
            data: arbOwnerEncodeFunctionData(params),
            value: BigInt(0),
        };
    }
    return {
        to: upgradeExecutor,
        data: (0, upgradeExecutorEncodeFunctionData_1.upgradeExecutorEncodeFunctionData)({
            functionName: 'executeCall',
            args: [
                ArbOwner_1.arbOwnerAddress,
                arbOwnerEncodeFunctionData(params), // targetCallData
            ],
        }),
        value: BigInt(0),
    };
}
exports.arbOwnerPrepareFunctionData = arbOwnerPrepareFunctionData;
async function arbOwnerPrepareTransactionRequest(client, params) {
    if (typeof client.chain === 'undefined') {
        throw new Error('[arbOwnerPrepareTransactionRequest] client.chain is undefined');
    }
    // params is extending ArbOwnerPrepareFunctionDataParameters, it's safe to cast
    const { to, data, value } = arbOwnerPrepareFunctionData({
        ...params,
        abi: ArbOwner_1.arbOwnerABI,
    });
    // @ts-ignore (todo: fix viem type issue)
    const request = await client.prepareTransactionRequest({
        chain: client.chain,
        to,
        data,
        value,
        account: params.account,
        // if the base gas limit override was provided, hardcode gas to 0 to skip estimation
        // we'll set the actual value in the code below
        gas: typeof params.gasOverrides?.gasLimit?.base !== 'undefined' ? 0n : undefined,
    });
    // potential gas overrides (gas limit)
    if (params.gasOverrides && params.gasOverrides.gasLimit) {
        request.gas = (0, gasOverrides_1.applyPercentIncrease)({
            // the ! is here because we should let it error in case we don't have the estimated gas
            base: params.gasOverrides.gasLimit.base ?? request.gas,
            percentIncrease: params.gasOverrides.gasLimit.percentIncrease,
        });
    }
    return { ...request, chainId: client.chain.id };
}
exports.arbOwnerPrepareTransactionRequest = arbOwnerPrepareTransactionRequest;

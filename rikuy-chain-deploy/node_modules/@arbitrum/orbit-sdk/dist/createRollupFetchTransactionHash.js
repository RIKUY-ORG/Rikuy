"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRollupFetchTransactionHash = exports.getRollupInitializedEvents = void 0;
const viem_1 = require("viem");
const getLogsWithBatching_1 = require("./utils/getLogsWithBatching");
const getEarliestRollupCreatorDeploymentBlockNumber_1 = require("./utils/getEarliestRollupCreatorDeploymentBlockNumber");
const Rollup_1 = require("./contracts/Rollup");
const v2_1_1 = require("./contracts/Rollup/v2.1");
const RollupInitializedEventAbi = {
    anonymous: false,
    inputs: [
        {
            indexed: false,
            internalType: 'bytes32',
            name: 'machineHash',
            type: 'bytes32',
        },
        {
            indexed: false,
            internalType: 'uint256',
            name: 'chainId',
            type: 'uint256',
        },
    ],
    name: 'RollupInitialized',
    type: 'event',
};
// it's the same between v2.1 and v3.1
const pausedEventSelector = (0, viem_1.getEventSelector)((0, viem_1.getAbiItem)({
    abi: Rollup_1.rollupABI,
    name: 'Paused',
}));
const userStakeUpdatedEventSelectorV3Dot1 = (0, viem_1.getEventSelector)((0, viem_1.getAbiItem)({
    abi: Rollup_1.rollupABI,
    name: 'UserStakeUpdated',
}));
const userStakeUpdatedEventSelectorV2Dot1 = (0, viem_1.getEventSelector)((0, viem_1.getAbiItem)({
    abi: v2_1_1.rollupABI,
    name: 'UserStakeUpdated',
}));
function getNextRollupQuery(txReceipt) {
    let pausedEventLog;
    let userStakeUpdatedEventLog;
    txReceipt.logs.forEach((log) => {
        const topic = log.topics[0];
        if (topic === pausedEventSelector) {
            pausedEventLog = log;
        }
        if (topic === userStakeUpdatedEventSelectorV3Dot1 ||
            topic === userStakeUpdatedEventSelectorV2Dot1) {
            userStakeUpdatedEventLog = log;
        }
    });
    // both events are there, so it's likely an upgrade
    if (typeof pausedEventLog !== 'undefined' && typeof userStakeUpdatedEventLog !== 'undefined') {
        return pausedEventLog.address;
    }
    return undefined;
}
async function getRollupInitializedEvents({ rollup, publicClient, fromBlock, }) {
    // Find the RollupInitialized event from that Rollup contract
    const rollupInitializedEvents = await (0, getLogsWithBatching_1.getLogsWithBatching)(publicClient, {
        address: rollup,
        event: RollupInitializedEventAbi,
        fromBlock: fromBlock ?? (0, getEarliestRollupCreatorDeploymentBlockNumber_1.getEarliestRollupCreatorDeploymentBlockNumber)(publicClient),
    });
    if (rollupInitializedEvents.length !== 1) {
        throw new Error(`Expected to find 1 RollupInitialized event for rollup address ${rollup} but found ${rollupInitializedEvents.length}`);
    }
    return rollupInitializedEvents;
}
exports.getRollupInitializedEvents = getRollupInitializedEvents;
async function createRollupFetchTransactionHash({ rollup, publicClient, fromBlock, }) {
    let rollupQuery = rollup;
    let transactionHash = undefined;
    while (rollupQuery) {
        // get the transaction hash and receipt for transaction that emitted the RollupInitialized event
        transactionHash = (await getRollupInitializedEvents({ rollup: rollupQuery, publicClient, fromBlock }))[0].transactionHash;
        const transactionReceipt = await publicClient.getTransactionReceipt({
            hash: transactionHash,
        });
        // we'll check the transaction receipt to see if it looks like a Rollup upgrade transaction, and return the next address to query
        // we'll keep following the chain of upgrades until we find the original deployment
        rollupQuery = getNextRollupQuery(transactionReceipt);
    }
    if (!transactionHash) {
        throw new Error(`No transactionHash found in RollupInitialized event for rollup address ${rollup}`);
    }
    return transactionHash;
}
exports.createRollupFetchTransactionHash = createRollupFetchTransactionHash;

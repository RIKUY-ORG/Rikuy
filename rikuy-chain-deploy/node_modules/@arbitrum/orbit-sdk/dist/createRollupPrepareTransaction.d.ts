import { Transaction } from 'viem';
import { CreateRollupFunctionInputs, RollupCreatorVersion, RollupCreatorLatestVersion } from './types/createRollupTypes';
export type CreateRollupTransaction = Transaction & {
    getInputs<TVersion extends RollupCreatorVersion = RollupCreatorLatestVersion>(): CreateRollupFunctionInputs<TVersion>;
};
export declare function createRollupPrepareTransaction(tx: Transaction): CreateRollupTransaction;

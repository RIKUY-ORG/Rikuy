"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createRollupPrepareTransactionReceipt = void 0;
const viem_1 = require("viem");
const RollupCreator_1 = require("./contracts/RollupCreator");
const v2_1_1 = require("./contracts/RollupCreator/v2.1");
function findRollupCreatedEventLog(txReceipt) {
    // v3.1
    const v3Dot1EventSelector = (0, viem_1.getEventSelector)((0, viem_1.getAbiItem)({ abi: RollupCreator_1.rollupCreatorABI, name: 'RollupCreated' }));
    // v2.1 and v1.1 are the same, so we only need to handle v2.1
    const v2Dot1EventSelector = (0, viem_1.getEventSelector)((0, viem_1.getAbiItem)({ abi: v2_1_1.rollupCreatorABI, name: 'RollupCreated' }));
    const log = txReceipt.logs.find((log) => {
        const topic = log.topics[0];
        return topic === v3Dot1EventSelector || topic === v2Dot1EventSelector;
    });
    if (typeof log === 'undefined') {
        throw new Error(`No "RollupCreated" logs found in logs for transaction: ${txReceipt.transactionHash}`);
    }
    return log;
}
function decodeRollupCreatedEventLog(log) {
    let decodedEventLog = null;
    // try parsing from multiple RollupCreator versions
    [
        // v3.1
        RollupCreator_1.rollupCreatorABI,
        // v2.1 and v1.1 are the same, so we only need to handle v2.1
        v2_1_1.rollupCreatorABI,
    ].forEach((abi) => {
        try {
            const result = (0, viem_1.decodeEventLog)({ ...log, abi });
            if (result.eventName === 'RollupCreated') {
                decodedEventLog = result;
            }
        }
        catch (error) {
            // do nothing
        }
    });
    if (decodedEventLog === null) {
        throw new Error(`Failed to decode "RollupCreated" event log: ${log}`);
    }
    return decodedEventLog;
}
function createRollupPrepareTransactionReceipt(txReceipt) {
    return {
        ...txReceipt,
        getCoreContracts: function () {
            const eventLog = findRollupCreatedEventLog(txReceipt);
            const decodedEventLog = decodeRollupCreatedEventLog(eventLog);
            const { rollupAddress, inboxAddress, ...rest } = decodedEventLog.args;
            return {
                rollup: rollupAddress,
                inbox: inboxAddress,
                ...rest,
                deployedAtBlockNumber: Number(txReceipt.blockNumber),
            };
        },
    };
}
exports.createRollupPrepareTransactionReceipt = createRollupPrepareTransactionReceipt;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createSafePrepareTransactionRequest = exports.SafeProxyFactoryAbi = void 0;
const viem_1 = require("viem");
const protocol_kit_1 = require("@safe-global/protocol-kit");
const utils_1 = require("@safe-global/protocol-kit/dist/src/contracts/utils");
const validateChain_1 = require("./utils/validateChain");
exports.SafeProxyFactoryAbi = [
    {
        anonymous: false,
        inputs: [
            {
                indexed: false,
                internalType: 'contract GnosisSafeProxy',
                name: 'proxy',
                type: 'address',
            },
            {
                indexed: false,
                internalType: 'address',
                name: 'singleton',
                type: 'address',
            },
        ],
        name: 'ProxyCreation',
        type: 'event',
    },
    {
        inputs: [
            {
                internalType: 'address',
                name: '_singleton',
                type: 'address',
            },
            {
                internalType: 'bytes',
                name: 'initializer',
                type: 'bytes',
            },
            {
                internalType: 'uint256',
                name: 'saltNonce',
                type: 'uint256',
            },
        ],
        name: 'createProxyWithNonce',
        outputs: [
            {
                internalType: 'contract GnosisSafeProxy',
                name: 'proxy',
                type: 'address',
            },
        ],
        stateMutability: 'nonpayable',
        type: 'function',
    },
];
/**
 * Prepares the transaction to create a new Safe using the default SafeFactory
 *
 * It leverages the [Protocol Kit](https://docs.safe.global/sdk/protocol-kit) from the Safe{Core} SDK.
 *
 * Returns the transaction to sign and send to the blockchain.
 *
 * @param {CreateSafePrepareTransactionRequestParams} createSafePrepareTransactionRequestParams {@link CreateSafePrepareTransactionRequestParams}
 * @param {PublicClient} createSafePrepareTransactionRequestParams.publicClient - A Viem Public Client
 * @param {PrivateKeyAccount} createSafePrepareTransactionRequestParams.account - The private key of the deployer of the new Safe
 * @param {Address[]} createSafePrepareTransactionRequestParams.owners - Array of addresses of the signers of the Safe
 * @param {number} createSafePrepareTransactionRequestParams.threshold - Number of signatures needed to validate a transaction in the Safe
 * @param {bigint} createSafePrepareTransactionRequestParams.saltNonce - Optional salt nonce for the call to Create2
 *
 * @returns Promise<{@link TransactionRequest}> - the transaction to sign and send to the blockchain.
 */
async function createSafePrepareTransactionRequest({ publicClient, account, owners, threshold, saltNonce, }) {
    const chainId = (0, validateChain_1.validateChain)(publicClient);
    // set and validate Safe configuration
    const safeAccountConfig = {
        owners,
        threshold,
    };
    (0, utils_1.validateSafeAccountConfig)(safeAccountConfig);
    // instantiate Safe Factory
    const safeFactory = await protocol_kit_1.SafeFactory.init({
        provider: publicClient.chain.rpcUrls.default.http[0],
    });
    // instantiate Safe Contract
    const safeContract = await (0, protocol_kit_1.getSafeContract)({
        safeProvider: safeFactory.getSafeProvider(),
        safeVersion: protocol_kit_1.DEFAULT_SAFE_VERSION,
    });
    // get Safe Proxy Factory address
    const safeProxyFactoryAddress = (await safeFactory.getAddress());
    // get Safe Contract address
    const safeContractAddress = (await safeContract.getAddress());
    // initializer calldata
    const initializer = (await (0, protocol_kit_1.encodeSetupCallData)({
        safeProvider: safeFactory.getSafeProvider(),
        safeAccountConfig,
        safeContract,
    }));
    // salt nonce
    if (!saltNonce) {
        saltNonce = BigInt((0, utils_1.getChainSpecificDefaultSaltNonce)(BigInt(chainId)));
    }
    // prepare the transaction request
    const request = await publicClient.prepareTransactionRequest({
        chain: publicClient.chain,
        to: safeProxyFactoryAddress,
        data: (0, viem_1.encodeFunctionData)({
            abi: exports.SafeProxyFactoryAbi,
            functionName: 'createProxyWithNonce',
            args: [
                safeContractAddress,
                initializer,
                saltNonce, // saltNonce
            ],
        }),
        account,
    });
    return { ...request, chainId };
}
exports.createSafePrepareTransactionRequest = createSafePrepareTransactionRequest;

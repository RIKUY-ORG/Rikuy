import { Address, PublicClient, Transport, Chain } from 'viem';
import { Prettify } from './types/utils';
import { WithTokenBridgeCreatorAddressOverride } from './types/createTokenBridgeTypes';
export type CreateTokenBridgeEnoughCustomFeeTokenAllowanceParams<TChain extends Chain | undefined> = Prettify<WithTokenBridgeCreatorAddressOverride<{
    nativeToken: Address;
    owner: Address;
    publicClient: PublicClient<Transport, TChain>;
}>>;
export declare function createTokenBridgeEnoughCustomFeeTokenAllowance<TChain extends Chain | undefined>({ nativeToken, owner, publicClient, tokenBridgeCreatorAddressOverride, }: CreateTokenBridgeEnoughCustomFeeTokenAllowanceParams<TChain>): Promise<boolean>;

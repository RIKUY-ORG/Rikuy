"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest = void 0;
const erc20_1 = require("./utils/erc20");
const ParentChain_1 = require("./types/ParentChain");
const getTokenBridgeCreatorAddress_1 = require("./utils/getTokenBridgeCreatorAddress");
const constants_1 = require("./constants");
const decimals_1 = require("./utils/decimals");
async function createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest({ amount, nativeToken, owner, publicClient, tokenBridgeCreatorAddressOverride, }) {
    const { chainId } = (0, ParentChain_1.validateParentChain)(publicClient);
    const decimals = await (0, erc20_1.fetchDecimals)({
        address: nativeToken,
        publicClient,
    });
    const request = await (0, erc20_1.approvePrepareTransactionRequest)({
        address: nativeToken,
        owner,
        spender: tokenBridgeCreatorAddressOverride ?? (0, getTokenBridgeCreatorAddress_1.getTokenBridgeCreatorAddress)(publicClient),
        amount: amount ??
            (0, decimals_1.scaleFrom18DecimalsToNativeTokenDecimals)({
                amount: constants_1.createTokenBridgeDefaultRetryablesFees,
                decimals,
            }),
        publicClient,
    });
    return { ...request, chainId };
}
exports.createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest = createTokenBridgePrepareCustomFeeTokenApprovalTransactionRequest;

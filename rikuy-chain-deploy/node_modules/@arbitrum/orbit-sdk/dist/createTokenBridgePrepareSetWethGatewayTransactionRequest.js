"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTokenBridgePrepareSetWethGatewayTransactionRequest = void 0;
const viem_1 = require("viem");
const isCustomFeeTokenChain_1 = require("./utils/isCustomFeeTokenChain");
const upgradeExecutorEncodeFunctionData_1 = require("./upgradeExecutorEncodeFunctionData");
const createTokenBridgeFetchTokenBridgeContracts_1 = require("./createTokenBridgeFetchTokenBridgeContracts");
const createRollupFetchCoreContracts_1 = require("./createRollupFetchCoreContracts");
const createTokenBridge_ethers_1 = require("./createTokenBridge-ethers");
const gasOverrides_1 = require("./utils/gasOverrides");
const ParentChain_1 = require("./types/ParentChain");
const registerNewNetwork_1 = require("./utils/registerNewNetwork");
const publicClientToProvider_1 = require("./ethers-compat/publicClientToProvider");
const parentChainGatewayRouterAbi = [
    {
        inputs: [
            {
                internalType: 'address',
                name: '',
                type: 'address',
            },
        ],
        name: 'l1TokenToGateway',
        outputs: [
            {
                internalType: 'address',
                name: '',
                type: 'address',
            },
        ],
        stateMutability: 'view',
        type: 'function',
    },
    {
        inputs: [
            {
                internalType: 'address[]',
                name: '_token',
                type: 'address[]',
            },
            {
                internalType: 'address[]',
                name: '_gateway',
                type: 'address[]',
            },
            {
                internalType: 'uint256',
                name: '_maxGas',
                type: 'uint256',
            },
            {
                internalType: 'uint256',
                name: '_gasPriceBid',
                type: 'uint256',
            },
            {
                internalType: 'uint256',
                name: '_maxSubmissionCost',
                type: 'uint256',
            },
        ],
        name: 'setGateways',
        outputs: [
            {
                internalType: 'uint256',
                name: '',
                type: 'uint256',
            },
        ],
        stateMutability: 'payable',
        type: 'function',
    },
];
async function createTokenBridgePrepareSetWethGatewayTransactionRequest({ rollup, rollupDeploymentBlockNumber, parentChainPublicClient, orbitChainPublicClient, account, retryableGasOverrides, tokenBridgeCreatorAddressOverride, }) {
    const { chainId } = (0, ParentChain_1.validateParentChain)(parentChainPublicClient);
    // Ensure that networks are registered
    await (0, registerNewNetwork_1.registerNewNetwork)((0, publicClientToProvider_1.publicClientToProvider)(parentChainPublicClient), (0, publicClientToProvider_1.publicClientToProvider)(orbitChainPublicClient), rollup);
    // check for custom fee token chain
    if (await (0, isCustomFeeTokenChain_1.isCustomFeeTokenChain)({
        rollup,
        parentChainPublicClient,
    })) {
        throw new Error('chain is custom fee token chain, no need to register the weth gateway.');
    }
    // get inbox address from rollup
    const inbox = await parentChainPublicClient.readContract({
        address: rollup,
        abi: (0, viem_1.parseAbi)(['function inbox() view returns (address)']),
        functionName: 'inbox',
    });
    // get token bridge contracts
    const tokenBridgeContracts = await (0, createTokenBridgeFetchTokenBridgeContracts_1.createTokenBridgeFetchTokenBridgeContracts)({
        inbox,
        parentChainPublicClient,
        tokenBridgeCreatorAddressOverride,
    });
    // check whether the weth gateway is already registered in the router
    const registeredWethGateway = await parentChainPublicClient.readContract({
        address: tokenBridgeContracts.parentChainContracts.router,
        abi: parentChainGatewayRouterAbi,
        functionName: 'l1TokenToGateway',
        args: [tokenBridgeContracts.parentChainContracts.weth],
    });
    if (registeredWethGateway === tokenBridgeContracts.parentChainContracts.wethGateway) {
        throw new Error('weth gateway is already registered in the router.');
    }
    const rollupCoreContracts = await (0, createRollupFetchCoreContracts_1.createRollupFetchCoreContracts)({
        rollup,
        rollupDeploymentBlockNumber,
        publicClient: parentChainPublicClient,
    });
    // encode data for the setGateways call
    // (we first encode dummy data, to get the retryable message estimates)
    const setGatewaysDummyCalldata = (0, viem_1.encodeFunctionData)({
        abi: parentChainGatewayRouterAbi,
        functionName: 'setGateways',
        args: [
            [tokenBridgeContracts.parentChainContracts.weth],
            [tokenBridgeContracts.parentChainContracts.wethGateway],
            1n,
            1n,
            1n, // _maxSubmissionCost
        ],
    });
    const retryableTicketGasEstimates = await (0, createTokenBridge_ethers_1.getEstimateForSettingGateway)(account, rollupCoreContracts.upgradeExecutor, tokenBridgeContracts.parentChainContracts.router, setGatewaysDummyCalldata, parentChainPublicClient, orbitChainPublicClient);
    //// apply gas overrides
    const gasLimit = retryableGasOverrides && retryableGasOverrides.gasLimit
        ? (0, gasOverrides_1.applyPercentIncrease)({
            base: retryableGasOverrides.gasLimit.base ?? retryableTicketGasEstimates.gasLimit,
            percentIncrease: retryableGasOverrides.gasLimit.percentIncrease,
        })
        : retryableTicketGasEstimates.gasLimit;
    const maxFeePerGas = retryableGasOverrides && retryableGasOverrides.maxFeePerGas
        ? (0, gasOverrides_1.applyPercentIncrease)({
            base: retryableGasOverrides.maxFeePerGas.base ?? retryableTicketGasEstimates.maxFeePerGas,
            percentIncrease: retryableGasOverrides.maxFeePerGas.percentIncrease,
        })
        : retryableTicketGasEstimates.maxFeePerGas;
    const maxSubmissionCost = retryableGasOverrides && retryableGasOverrides.maxSubmissionCost
        ? (0, gasOverrides_1.applyPercentIncrease)({
            base: retryableGasOverrides.maxSubmissionCost.base ??
                retryableTicketGasEstimates.maxSubmissionCost,
            percentIncrease: retryableGasOverrides.maxSubmissionCost.percentIncrease,
        })
        : retryableTicketGasEstimates.maxSubmissionCost;
    const deposit = gasLimit * maxFeePerGas + maxSubmissionCost;
    // (and then we encode the real data, to send the transaction)
    const setGatewaysCalldata = (0, viem_1.encodeFunctionData)({
        abi: parentChainGatewayRouterAbi,
        functionName: 'setGateways',
        args: [
            [tokenBridgeContracts.parentChainContracts.weth],
            [tokenBridgeContracts.parentChainContracts.wethGateway],
            gasLimit,
            maxFeePerGas,
            maxSubmissionCost, // _maxSubmissionCost
        ],
    });
    // prepare the transaction request with a call to the upgrade executor
    // @ts-ignore (todo: fix viem type issue)
    const request = await parentChainPublicClient.prepareTransactionRequest({
        chain: parentChainPublicClient.chain,
        to: rollupCoreContracts.upgradeExecutor,
        data: (0, upgradeExecutorEncodeFunctionData_1.upgradeExecutorEncodeFunctionData)({
            functionName: 'executeCall',
            args: [
                tokenBridgeContracts.parentChainContracts.router,
                setGatewaysCalldata, // targetCallData
            ],
        }),
        value: deposit,
        account,
    });
    return { ...request, chainId };
}
exports.createTokenBridgePrepareSetWethGatewayTransactionRequest = createTokenBridgePrepareSetWethGatewayTransactionRequest;

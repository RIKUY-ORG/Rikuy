"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTokenBridgePrepareTransactionRequest = void 0;
const viem_1 = require("viem");
const TokenBridgeCreator_1 = require("./contracts/TokenBridgeCreator");
const ParentChain_1 = require("./types/ParentChain");
const createTokenBridge_ethers_1 = require("./createTokenBridge-ethers");
const isCustomFeeTokenChain_1 = require("./utils/isCustomFeeTokenChain");
const gasOverrides_1 = require("./utils/gasOverrides");
const getTokenBridgeCreatorAddress_1 = require("./utils/getTokenBridgeCreatorAddress");
const isTokenBridgeDeployed_1 = require("./isTokenBridgeDeployed");
async function createTokenBridgePrepareTransactionRequest({ params, parentChainPublicClient, orbitChainPublicClient, account, gasOverrides, retryableGasOverrides, tokenBridgeCreatorAddressOverride, }) {
    const { chainId } = (0, ParentChain_1.validateParentChain)(parentChainPublicClient);
    const isTokenBridgeAlreadyDeployed = await (0, isTokenBridgeDeployed_1.isTokenBridgeDeployed)({
        parentChainPublicClient,
        orbitChainPublicClient,
        rollup: params.rollup,
        tokenBridgeCreatorAddressOverride,
    });
    if (isTokenBridgeAlreadyDeployed) {
        throw new Error(`Token bridge contracts for Rollup ${params.rollup} are already deployed`);
    }
    const tokenBridgeCreatorAddress = tokenBridgeCreatorAddressOverride ?? (0, getTokenBridgeCreatorAddress_1.getTokenBridgeCreatorAddress)(parentChainPublicClient);
    const { inbox, maxGasForContracts, maxGasPrice, retryableFee } = await (0, createTokenBridge_ethers_1.createTokenBridgeGetInputs)(account, parentChainPublicClient, orbitChainPublicClient, tokenBridgeCreatorAddress, params.rollup, retryableGasOverrides);
    const chainUsesCustomFee = await (0, isCustomFeeTokenChain_1.isCustomFeeTokenChain)({
        rollup: params.rollup,
        parentChainPublicClient,
    });
    // @ts-ignore (todo: fix viem type issue)
    const request = await parentChainPublicClient.prepareTransactionRequest({
        chain: parentChainPublicClient.chain,
        to: tokenBridgeCreatorAddress,
        data: (0, viem_1.encodeFunctionData)({
            abi: TokenBridgeCreator_1.tokenBridgeCreatorABI,
            functionName: 'createTokenBridge',
            args: [inbox, params.rollupOwner, maxGasForContracts, maxGasPrice],
        }),
        value: chainUsesCustomFee ? 0n : retryableFee,
        account: account,
        // if the base gas limit override was provided, hardcode gas to 0 to skip estimation
        // we'll set the actual value in the code below
        gas: typeof gasOverrides?.gasLimit?.base !== 'undefined' ? 0n : undefined,
    });
    // potential gas overrides (gas limit)
    if (gasOverrides && gasOverrides.gasLimit) {
        request.gas = (0, gasOverrides_1.applyPercentIncrease)({
            // the ! is here because we should let it error in case we don't have the estimated gas
            base: gasOverrides.gasLimit.base ?? request.gas,
            percentIncrease: gasOverrides.gasLimit.percentIncrease,
        });
    }
    return { ...request, chainId };
}
exports.createTokenBridgePrepareTransactionRequest = createTokenBridgePrepareTransactionRequest;

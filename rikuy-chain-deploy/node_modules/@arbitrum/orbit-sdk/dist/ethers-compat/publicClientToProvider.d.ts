import { PublicClient, Transport, Chain } from 'viem';
import { providers } from 'ethers';
export declare function publicClientToProvider<TChain extends Chain | undefined>(publicClient: PublicClient<Transport, TChain>): providers.StaticJsonRpcProvider;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const viem_1 = require("viem");
const accounts_1 = require("viem/accounts");
const chains_1 = require("./chains");
const sequencerInboxActions_1 = require("./decorators/sequencerInboxActions");
const testHelpers_1 = require("./testHelpers");
const getBatchPosters_1 = require("./getBatchPosters");
const { l3RollupOwner } = (0, testHelpers_1.getNitroTestnodePrivateKeyAccounts)();
const { l3Rollup, l3UpgradeExecutor, l3SequencerInbox } = (0, testHelpers_1.getInformationFromTestnode)();
const client = (0, viem_1.createPublicClient)({
    chain: chains_1.nitroTestnodeL2,
    transport: (0, viem_1.http)(),
}).extend((0, sequencerInboxActions_1.sequencerInboxActions)({
    sequencerInbox: l3SequencerInbox,
}));
async function setBatchPoster(batchPoster, state) {
    const tx = await client.sequencerInboxPrepareTransactionRequest({
        functionName: 'setIsBatchPoster',
        args: [batchPoster, state],
        account: l3RollupOwner.address,
        upgradeExecutor: l3UpgradeExecutor,
        sequencerInbox: l3SequencerInbox,
    });
    const txHash = await client.sendRawTransaction({
        serializedTransaction: await l3RollupOwner.signTransaction(tx),
    });
    await client.waitForTransactionReceipt({
        hash: txHash,
    });
}
// Tests can be enabled once we run one node per integration test
(0, vitest_1.describe)('successfully get batch posters', () => {
    (0, vitest_1.it)('when disabling the same batch posters multiple time', async () => {
        const randomAccount = (0, accounts_1.privateKeyToAccount)((0, accounts_1.generatePrivateKey)()).address;
        const { isAccurate: isAccurateInitially, batchPosters: initialBatchPosters } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        // By default, chains from nitro testnode has 1 batch poster
        (0, vitest_1.expect)(initialBatchPosters).toHaveLength(1);
        (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
        await setBatchPoster(randomAccount, false);
        await setBatchPoster(randomAccount, false);
        const { isAccurate: isStillAccurate, batchPosters: newBatchPosters } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        // Setting the same batch poster multiple time to false doesn't add new batch posters
        (0, vitest_1.expect)(newBatchPosters).toEqual(initialBatchPosters);
        (0, vitest_1.expect)(isStillAccurate).toBeTruthy();
        await setBatchPoster(randomAccount, true);
        const { batchPosters, isAccurate } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        (0, vitest_1.expect)(batchPosters).toEqual(initialBatchPosters.concat(randomAccount));
        (0, vitest_1.expect)(isAccurate).toBeTruthy();
        // Reset state for future tests
        await setBatchPoster(randomAccount, false);
        const { isAccurate: isAccurateFinal, batchPosters: batchPostersFinal } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        (0, vitest_1.expect)(batchPostersFinal).toEqual(initialBatchPosters);
        (0, vitest_1.expect)(isAccurateFinal).toBeTruthy();
    });
    (0, vitest_1.it)('when enabling the same batch poster multiple time', async () => {
        const randomAccount = (0, accounts_1.privateKeyToAccount)((0, accounts_1.generatePrivateKey)()).address;
        const { isAccurate: isAccurateInitially, batchPosters: initialBatchPosters } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        // By default, chains from nitro testnode has 1 batch poster
        (0, vitest_1.expect)(initialBatchPosters).toHaveLength(1);
        (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
        await setBatchPoster(randomAccount, true);
        await setBatchPoster(randomAccount, true);
        const { isAccurate: isStillAccurate, batchPosters: newBatchPosters } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        (0, vitest_1.expect)(newBatchPosters).toEqual(initialBatchPosters.concat(randomAccount));
        (0, vitest_1.expect)(isStillAccurate).toBeTruthy();
        // Reset state for futures tests
        await setBatchPoster(randomAccount, false);
        const { batchPosters, isAccurate } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        (0, vitest_1.expect)(batchPosters).toEqual(initialBatchPosters);
        (0, vitest_1.expect)(isAccurate).toBeTruthy();
    });
    (0, vitest_1.it)('when adding an existing batch poster', async () => {
        const { isAccurate: isAccurateInitially, batchPosters: initialBatchPosters } = await (0, getBatchPosters_1.getBatchPosters)(client, { rollup: l3Rollup, sequencerInbox: l3SequencerInbox });
        (0, vitest_1.expect)(initialBatchPosters).toHaveLength(1);
        (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
        const firstBatchPoster = initialBatchPosters[0];
        await setBatchPoster(firstBatchPoster, true);
        const { isAccurate, batchPosters } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        (0, vitest_1.expect)(batchPosters).toEqual(initialBatchPosters);
        (0, vitest_1.expect)(isAccurate).toBeTruthy();
    });
    (0, vitest_1.it)('when removing an existing batch poster', async () => {
        const { isAccurate: isAccurateInitially, batchPosters: initialBatchPosters } = await (0, getBatchPosters_1.getBatchPosters)(client, { rollup: l3Rollup, sequencerInbox: l3SequencerInbox });
        (0, vitest_1.expect)(initialBatchPosters).toHaveLength(1);
        (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
        const lastBatchPoster = initialBatchPosters[initialBatchPosters.length - 1];
        await setBatchPoster(lastBatchPoster, false);
        const { isAccurate, batchPosters } = await (0, getBatchPosters_1.getBatchPosters)(client, {
            rollup: l3Rollup,
            sequencerInbox: l3SequencerInbox,
        });
        (0, vitest_1.expect)(batchPosters).toEqual(initialBatchPosters.slice(0, -1));
        (0, vitest_1.expect)(isAccurate).toBeTruthy();
        await setBatchPoster(lastBatchPoster, true);
        const { isAccurate: isAccurateFinal, batchPosters: batchPostersFinal } = await (0, getBatchPosters_1.getBatchPosters)(client, { rollup: l3Rollup, sequencerInbox: l3SequencerInbox });
        (0, vitest_1.expect)(batchPostersFinal).toEqual(initialBatchPosters);
        (0, vitest_1.expect)(isAccurateFinal).toBeTruthy();
    });
});

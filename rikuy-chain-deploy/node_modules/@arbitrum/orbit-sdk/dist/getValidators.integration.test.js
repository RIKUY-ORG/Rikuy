"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const viem_1 = require("viem");
const accounts_1 = require("viem/accounts");
const chains_1 = require("./chains");
const rollupAdminLogicPublicActions_1 = require("./decorators/rollupAdminLogicPublicActions");
const testHelpers_1 = require("./testHelpers");
const getValidators_1 = require("./getValidators");
const { l3RollupOwner } = (0, testHelpers_1.getNitroTestnodePrivateKeyAccounts)();
const { l3Rollup, l3UpgradeExecutor } = (0, testHelpers_1.getInformationFromTestnode)();
const rollupCreatorVersion = (0, testHelpers_1.testHelper_getRollupCreatorVersionFromEnv)();
// https://github.com/OffchainLabs/nitro-testnode/blob/release/test-node.bash#L634
// https://github.com/OffchainLabs/nitro-contracts/blob/v3.1.1/scripts/rollupCreation.ts#L250-L257
// https://github.com/OffchainLabs/nitro-contracts/blob/v2.1.3/scripts/rollupCreation.ts#L237-L243
const expectedInitialValidators = rollupCreatorVersion === 'v3.1' ? 11 : 10;
const client = (0, viem_1.createPublicClient)({
    chain: chains_1.nitroTestnodeL2,
    transport: (0, viem_1.http)(),
}).extend((0, rollupAdminLogicPublicActions_1.rollupAdminLogicPublicActions)({
    rollup: l3Rollup,
}));
async function setValidator(validator, state) {
    const tx = await client.rollupAdminLogicPrepareTransactionRequest({
        functionName: 'setValidator',
        args: [[validator], [state]],
        account: l3RollupOwner.address,
        upgradeExecutor: l3UpgradeExecutor,
        rollup: l3Rollup,
    });
    const txHash = await client.sendRawTransaction({
        serializedTransaction: await l3RollupOwner.signTransaction(tx),
    });
    await client.waitForTransactionReceipt({
        hash: txHash,
    });
}
// Tests can be enabled once we run one node per integration test
(0, vitest_1.describe)('successfully get validators', () => {
    (0, vitest_1.it)('when disabling the same validator multiple times', async () => {
        const randomAccount = (0, accounts_1.privateKeyToAccount)((0, accounts_1.generatePrivateKey)()).address;
        const { isAccurate: isAccurateInitially, validators: initialValidators } = await (0, getValidators_1.getValidators)(client, {
            rollup: l3Rollup,
        });
        (0, vitest_1.expect)(initialValidators).toHaveLength(expectedInitialValidators);
        (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
        await setValidator(randomAccount, false);
        await setValidator(randomAccount, false);
        const { isAccurate: isStillAccurate, validators: newValidators } = await (0, getValidators_1.getValidators)(client, {
            rollup: l3Rollup,
        });
        // Setting the same validator multiple time to false doesn't add new validators
        (0, vitest_1.expect)(newValidators).toEqual(initialValidators);
        (0, vitest_1.expect)(isStillAccurate).toBeTruthy();
        await setValidator(randomAccount, true);
        const { validators, isAccurate } = await (0, getValidators_1.getValidators)(client, { rollup: l3Rollup });
        (0, vitest_1.expect)(validators).toEqual(initialValidators.concat(randomAccount));
        (0, vitest_1.expect)(isAccurate).toBeTruthy();
        // Reset state for future tests
        await setValidator(randomAccount, false);
        const { isAccurate: isAccurateFinal, validators: validatorsFinal } = await (0, getValidators_1.getValidators)(client, {
            rollup: l3Rollup,
        });
        (0, vitest_1.expect)(validatorsFinal).toEqual(initialValidators);
        (0, vitest_1.expect)(isAccurateFinal).toBeTruthy();
    });
    (0, vitest_1.it)('when enabling the same validators multiple times', async () => {
        const randomAccount = (0, accounts_1.privateKeyToAccount)((0, accounts_1.generatePrivateKey)()).address;
        const { isAccurate: isAccurateInitially, validators: initialValidators } = await (0, getValidators_1.getValidators)(client, {
            rollup: l3Rollup,
        });
        (0, vitest_1.expect)(initialValidators).toHaveLength(expectedInitialValidators);
        (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
        await setValidator(randomAccount, true);
        await setValidator(randomAccount, true);
        const { isAccurate: isStillAccurate, validators: newValidators } = await (0, getValidators_1.getValidators)(client, {
            rollup: l3Rollup,
        });
        (0, vitest_1.expect)(newValidators).toEqual(initialValidators.concat(randomAccount));
        (0, vitest_1.expect)(isStillAccurate).toBeTruthy();
        // Reset state for futures tests
        await setValidator(randomAccount, false);
        const { validators, isAccurate } = await (0, getValidators_1.getValidators)(client, { rollup: l3Rollup });
        (0, vitest_1.expect)(validators).toEqual(initialValidators);
        (0, vitest_1.expect)(isAccurate).toBeTruthy();
    });
    (0, vitest_1.it)('when adding an existing validator', async () => {
        const { isAccurate: isAccurateInitially, validators: initialValidators } = await (0, getValidators_1.getValidators)(client, { rollup: l3Rollup });
        (0, vitest_1.expect)(initialValidators).toHaveLength(expectedInitialValidators);
        (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
        const firstValidator = initialValidators[0];
        await setValidator(firstValidator, true);
        const { isAccurate, validators } = await (0, getValidators_1.getValidators)(client, { rollup: l3Rollup });
        (0, vitest_1.expect)(validators).toEqual(initialValidators);
        (0, vitest_1.expect)(isAccurate).toBeTruthy();
    });
    (0, vitest_1.it)('when removing an existing validator', async () => {
        const { isAccurate: isAccurateInitially, validators: initialValidators } = await (0, getValidators_1.getValidators)(client, { rollup: l3Rollup });
        (0, vitest_1.expect)(initialValidators).toHaveLength(expectedInitialValidators);
        (0, vitest_1.expect)(isAccurateInitially).toBeTruthy();
        const lastValidator = initialValidators[initialValidators.length - 1];
        await setValidator(lastValidator, false);
        const { isAccurate, validators } = await (0, getValidators_1.getValidators)(client, { rollup: l3Rollup });
        (0, vitest_1.expect)(validators).toEqual(initialValidators.slice(0, -1));
        (0, vitest_1.expect)(isAccurate).toBeTruthy();
        await setValidator(lastValidator, true);
        const { isAccurate: isAccurateFinal, validators: validatorsFinal } = await (0, getValidators_1.getValidators)(client, { rollup: l3Rollup });
        (0, vitest_1.expect)(validatorsFinal).toEqual(initialValidators);
        (0, vitest_1.expect)(isAccurateFinal).toBeTruthy();
    });
});

import { Address, Chain, PublicClient, Transport } from 'viem';
export declare function isAnyTrust<TChain extends Chain>({ rollup, publicClient, }: {
    rollup: Address;
    publicClient: PublicClient<Transport, TChain>;
}): Promise<never>;

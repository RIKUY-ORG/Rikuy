"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.rollupAdminLogicPrepareTransactionRequest = exports.rollupAdminLogicPrepareFunctionData = void 0;
const viem_1 = require("viem");
const Rollup_1 = require("./contracts/Rollup");
const upgradeExecutorEncodeFunctionData_1 = require("./upgradeExecutorEncodeFunctionData");
const ParentChain_1 = require("./types/ParentChain");
function rollupAdminLogicEncodeFunctionData({ abi, functionName, args, }) {
    return (0, viem_1.encodeFunctionData)({
        abi,
        functionName,
        args,
    });
}
function rollupAdminLogicPrepareFunctionData(params) {
    const { upgradeExecutor } = params;
    if (!upgradeExecutor) {
        return {
            to: params.rollup,
            data: rollupAdminLogicEncodeFunctionData(params),
            value: BigInt(0),
        };
    }
    return {
        to: upgradeExecutor,
        data: (0, upgradeExecutorEncodeFunctionData_1.upgradeExecutorEncodeFunctionData)({
            functionName: 'executeCall',
            args: [
                params.rollup,
                rollupAdminLogicEncodeFunctionData(params), // targetCallData
            ],
        }),
        value: BigInt(0),
    };
}
exports.rollupAdminLogicPrepareFunctionData = rollupAdminLogicPrepareFunctionData;
async function rollupAdminLogicPrepareTransactionRequest(client, params) {
    const { chainId } = (0, ParentChain_1.validateParentChain)(client);
    // params is extending RollupAdminLogicPrepareFunctionDataParameters, it's safe to cast
    const { to, data, value } = rollupAdminLogicPrepareFunctionData({
        ...params,
        abi: Rollup_1.rollupABI,
    });
    // @ts-ignore (todo: fix viem type issue)
    const request = await client.prepareTransactionRequest({
        chain: client.chain,
        to,
        data,
        value,
        account: params.account,
    });
    return { ...request, chainId };
}
exports.rollupAdminLogicPrepareTransactionRequest = rollupAdminLogicPrepareTransactionRequest;

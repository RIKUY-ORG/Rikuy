import { Client, Transport, Chain } from 'viem';
import { chains, nitroTestnodeL3 } from '../chains';
export type ParentChain = Exclude<(typeof chains)[number], {
    id: typeof nitroTestnodeL3.id;
}>;
export type ParentChainId = ParentChain['id'];
export declare function validateParentChain<TChain extends Chain | undefined>(chainIdOrClient: number | Client<Transport, TChain>): {
    chainId: number;
    isCustom: true;
} | {
    chainId: ParentChainId;
    isCustom: false;
};

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTokenBridgeCreatorAddress = void 0;
const TokenBridgeCreator_1 = require("../contracts/TokenBridgeCreator");
const ParentChain_1 = require("../types/ParentChain");
function getTokenBridgeCreatorAddress(client) {
    const { chainId: parentChainId, isCustom: parentChainIsCustom } = (0, ParentChain_1.validateParentChain)(client);
    if (parentChainIsCustom) {
        const contract = client.chain?.contracts?.tokenBridgeCreator;
        const address = contract?.address;
        if (typeof address === 'undefined') {
            throw new Error(`Address for TokenBridgeCreator is missing on custom parent chain with id ${parentChainId}`);
        }
        return address;
    }
    return TokenBridgeCreator_1.tokenBridgeCreatorAddress[parentChainId];
}
exports.getTokenBridgeCreatorAddress = getTokenBridgeCreatorAddress;

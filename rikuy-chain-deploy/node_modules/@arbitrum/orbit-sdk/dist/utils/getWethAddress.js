"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getWethAddress = void 0;
const WETH_1 = require("../contracts/WETH");
const ParentChain_1 = require("../types/ParentChain");
function getWethAddress(client) {
    const { chainId: parentChainId, isCustom: parentChainIsCustom } = (0, ParentChain_1.validateParentChain)(client);
    if (parentChainIsCustom) {
        const contract = client.chain?.contracts?.weth;
        const address = contract?.address;
        if (typeof address === 'undefined') {
            throw new Error(`Address for WETH is missing on custom parent chain with id ${parentChainId}`);
        }
        return address;
    }
    return WETH_1.wethAddress[parentChainId];
}
exports.getWethAddress = getWethAddress;

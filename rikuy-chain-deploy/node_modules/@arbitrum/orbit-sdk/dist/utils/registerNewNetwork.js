"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.registerNewNetwork = exports.prepareArbitrumNetwork = void 0;
const sdk_1 = require("@arbitrum/sdk");
const chains_1 = require("../chains");
const createRollupFetchTransactionHash_1 = require("../createRollupFetchTransactionHash");
const publicClientToProvider_1 = require("../ethers-compat/publicClientToProvider");
const createTokenBridgeFetchTokenBridgeContracts_1 = require("../createTokenBridgeFetchTokenBridgeContracts");
const isTestnet = (parentChainId) => {
    return chains_1.testnets.some((testnet) => testnet.id === parentChainId);
};
async function prepareArbitrumNetwork(parentChainPublicClient, { rollup }) {
    const rollupInitializedEvent = await (0, createRollupFetchTransactionHash_1.getRollupInitializedEvents)({
        rollup,
        publicClient: parentChainPublicClient,
    });
    // Fetch orbit chain chainId
    const { chainId } = rollupInitializedEvent[0].args;
    if (!chainId) {
        throw new Error("RollupInitialized event doesn't contain chainId");
    }
    const { parentChainId, ethBridge, confirmPeriodBlocks, nativeToken } = await (0, sdk_1.getArbitrumNetworkInformationFromRollup)(rollup, (0, publicClientToProvider_1.publicClientToProvider)(parentChainPublicClient));
    const { parentChainContracts, orbitChainContracts } = await (0, createTokenBridgeFetchTokenBridgeContracts_1.createTokenBridgeFetchTokenBridgeContracts)({
        inbox: ethBridge.inbox,
        parentChainPublicClient,
    });
    return {
        name: String(`${chainId}-arbitrum-network`),
        chainId: Number(chainId),
        parentChainId,
        confirmPeriodBlocks,
        ethBridge,
        isCustom: true,
        isTestnet: isTestnet(parentChainId),
        nativeToken,
        tokenBridge: {
            parentGatewayRouter: parentChainContracts.router,
            parentErc20Gateway: parentChainContracts.standardGateway,
            parentCustomGateway: parentChainContracts.customGateway,
            parentWeth: parentChainContracts.weth,
            parentWethGateway: parentChainContracts.wethGateway,
            parentMultiCall: parentChainContracts.multicall,
            childGatewayRouter: orbitChainContracts.router,
            childErc20Gateway: orbitChainContracts.standardGateway,
            childCustomGateway: orbitChainContracts.customGateway,
            childWeth: orbitChainContracts.weth,
            childWethGateway: orbitChainContracts.wethGateway,
            childMultiCall: orbitChainContracts.multicall,
        },
    };
}
exports.prepareArbitrumNetwork = prepareArbitrumNetwork;
const registerNewNetwork = async (parentProvider, childProvider, rollupAddress) => {
    const chainId = (await childProvider.getNetwork()).chainId;
    const { parentChainId, ethBridge, confirmPeriodBlocks } = await (0, sdk_1.getArbitrumNetworkInformationFromRollup)(rollupAddress, parentProvider);
    const arbitrumNetwork = {
        name: String(`${chainId}-arbitrum-network`),
        chainId,
        parentChainId,
        confirmPeriodBlocks,
        ethBridge,
        isCustom: true,
        isTestnet: isTestnet(parentChainId),
    };
    return (0, sdk_1.registerCustomArbitrumNetwork)(arbitrumNetwork);
};
exports.registerNewNetwork = registerNewNetwork;
